---
id: Q078
title: "Camera Intrinsic 和 Extrinsic 分别是什么？"
chapter: 8
chapter_title: "视频、3D 与多模态"
difficulty: 3
frequency: 4
top30: false
tags: ["video-3d-multimodal", "camera-geometry"]
source_label: "基础高频"
---

# Q078 · Camera Intrinsic 和 Extrinsic 分别是什么？

[← Q077](q077.md) [章节首页](README.md) [Q079 →](q079.md)

## 题目画像

- **章节**：视频、3D 与多模态
- **难度**：★★★
- **面试频度**：★★★★
- **来源标签**：基础高频
- **一句话记忆**：外参管相机在哪，内参管怎么投到像素。

## 30 秒回答

Extrinsic 把世界坐标变到相机坐标；Intrinsic 把相机坐标投到像素坐标。

如果只能再补一句：**外参管相机在哪，内参管怎么投到像素。**

## 面试官真正想听什么

这题不是只检查名词记忆，而是在看你能否把**机制、边界条件和工程后果**连起来。一个高质量回答至少覆盖下面三个层次：

1. **定义/结构**：先说明对象是什么，避免一上来堆历史或模块名。
2. **为什么有效**：用关键公式或信息流解释机制，而不是只说“效果更好”。
3. **什么时候会失效**：主动给出 trade-off、数据条件与部署注意事项。

## 2-5 分钟标准回答

### 1. 核心要点

P_c=RP_w+t 描述 pose

**深入理解：** 视频/3D/多模态题强调新增约束：时间一致性、几何投影、传感器同步和跨模态对齐。回答时要明确哪些信息来自观测，哪些依赖先验。

### 2. 核心要点

λp=K[R∣t]P_w，K 包含 fx,fy,cx,cy 等

**深入理解：** 视频/3D/多模态题强调新增约束：时间一致性、几何投影、传感器同步和跨模态对齐。回答时要明确哪些信息来自观测，哪些依赖先验。

### 3. 核心要点

真实相机还要标定 radial/tangential distortion；多相机系统最常见 bug 是坐标系方向和手性约定

**深入理解：** 视频/3D/多模态题强调新增约束：时间一致性、几何投影、传感器同步和跨模态对齐。回答时要明确哪些信息来自观测，哪些依赖先验。

## 关键公式 / 数学口径

针孔相机模型：

$$
P_c=RP_w+t,\qquad \tilde p\sim KP_c
$$

$K$ 是内参，$R,t$ 是从世界坐标到相机坐标的外参。

## 深入解析：从原理到工程

视频/3D/多模态题强调新增约束：时间一致性、几何投影、传感器同步和跨模态对齐。回答时要明确哪些信息来自观测，哪些依赖先验。

对 **Q078** 尤其要注意：机械震动/镜头更换会造成外参漂移，应版本化 calibration。 这不是附加知识，而是把理论答案变成“可上线判断”的关键一步。

### 推荐的口述顺序

```text
一句定义 → 关键公式/结构 → 设计动机 → 优势 → 局限/失败模式 → 工程验证
```

## 高频追问与参考答案

### 追问 1：为什么 t 不等于简单相机世界坐标？

外参中的 t 是坐标变换参数，具体表示取决于“world→camera”还是“camera→world”约定；它不总等于相机中心的世界坐标，后者常为 $C=-R^Tt$。

**回答加分点：** 最后补一句它会如何改变指标、训练稳定性或部署成本；若答案依赖实现约定，先声明约定。

### 追问 2：PnP 在求什么？

给定若干 3D 世界点及其 2D 图像投影和相机内参，PnP 估计相机姿态 R,t。

**回答加分点：** 最后补一句它会如何改变指标、训练稳定性或部署成本；若答案依赖实现约定，先声明约定。

## 常见失分点

- world→camera 与 camera→world 不说明清楚，公式很容易反。
- 只背结论，不解释“为什么”，或没有指出结论成立的前提。
- 论文/离线结论直接套到自己的数据，却没有给出切片实验或消融。

## 工程 / 项目视角

机械震动/镜头更换会造成外参漂移，应版本化 calibration。

建议把自己的项目补成以下四个事实：

- **数据条件**：这个机制在哪些尺寸/类别/光照/噪声/设备上最重要？
- **评价切片**：不能只看总体均值，至少有一个与本题机制直接相关的 slice。
- **成本**：训练显存、参数量、FLOPs、目标硬件 latency、前后处理成本至少知道一个。
- **回归**：新增方案让哪些场景变差？上线前如何兜底？

## V2 专业深化

> 这一节是在 PDF/原题解基础上的**专业扩展**：用于把“会答”提升到“能推导、能诊断、能做工程决策”。其中的扩展内容属于通用计算机视觉与工程知识，不用于声称某家公司曾逐字问过这些内容。

### 1. 更精确的技术表述

- 外参 $R,t$ 把世界坐标变换到相机坐标，内参 $K$ 把归一化相机坐标映射到像素坐标；完整 pinhole 模型还应考虑透镜畸变。
- 坐标系约定（右手/左手、world-to-camera/camera-to-world）最容易出错；写公式前必须声明变换方向。
- 标定误差会系统性影响深度、BEV 和多传感器融合，应通过 reprojection error 与下游几何误差共同验证。

### 2. 回答时必须显式声明的前提

- 视频先明确时间采样与 motion；3D 先明确坐标系与几何；多模态先明确同步/标定与可靠性。
- 跨帧/跨模态融合不仅是网络结构，还包含不同频率、延迟、缺失和噪声传播。
- 部署时应评估长序列状态、memory、传感器失效与时序一致性。

面试中尤其建议先把输入/输出、坐标或 tensor shape、训练/推理阶段以及评价口径说清楚。很多看似“知识点错误”的争论，本质是双方默认的实现约定不同。

### 3. 从“结论”推进到“机制”的推理链

针对 **Camera Intrinsic 和 Extrinsic 分别是什么？**，推荐强制自己完成以下链条：

```text
现象/任务约束
  → 数学对象或网络中的信息流
  → 为什么当前设计能改变梯度/表示/几何/搜索空间
  → 得到什么收益
  → 付出什么代价
  → 哪些数据分布下会失效
  → 用什么切片实验和 profiler 证明
```

不要把“论文上更好”当作最后一层证据。真正专业的答案需要把方法映射到**具体 failure mode**，再设计能证伪自己的实验。

### 4. 工程诊断矩阵

| 观察到的现象 | 优先怀疑 | 最小验证实验 |
|---|---|---|
| 时序抖动/漂移 | 独立帧预测、memory 错误累积 | 长序列可视化并按遮挡/重现切片 |
| 融合后反而变差 | 标定/同步/模态噪声 | modality ablation 与人为扰动测试 |

如果这两行都无法解释项目中的真实失败，再继续向数据版本、标注协议、随机性、导出后端或线上流量结构扩展，而不是直接堆更多模型模块。

### 5. 建议做的最小消融

- **Control**：保留原 baseline 的训练预算、数据版本、输入尺寸和评测脚本，只改变与本题直接相关的一项。
- **Slice**：至少增加一个与本题机制直接相关的子集指标；例如尺寸、遮挡、边界、长尾、噪声或设备域。
- **Cost**：同时记录参数量/FLOPs 之外的真实成本：显存、wall-clock、目标硬件 latency、P95/P99 或后处理开销。
- **Regression**：列出方案新增的 FP/FN 或退化场景，避免只展示改善样本。
- **Repeatability**：若提升幅度较小，使用多随机种子或 bootstrap 置信区间，判断收益是否超出自然波动。

### 6. 专家级追问清单

1. **为什么 t 不等于简单相机世界坐标？**：先给结论，再说明一个成立前提、一个反例/边界和一个可验证实验。
2. **PnP 在求什么？**：先给结论，再说明一个成立前提、一个反例/边界和一个可验证实验。
3. **标定或时间同步引入小扰动时，误差如何传播？**：先给结论，再说明一个成立前提、一个反例/边界和一个可验证实验。
4. **如果一个模态临时缺失，系统应如何优雅退化？**：先给结论，再说明一个成立前提、一个反例/边界和一个可验证实验。

### 7. 面试表达升级模板

> “我先明确这里的实现/评价口径。核心机制是 **A**，它通过 **B** 改变了 **C**，所以在 **D 类数据**上预期有效；代价是 **E**。如果这是我的项目，我会用 **一个控制实验 + 一个关键切片 + 一个真实硬件/线上指标** 来验证，而不是只看总体平均分。”

把这个模板替换成当前题目的具体名词后，通常可以自然从 30 秒回答扩展到 3–5 分钟，并且给面试官留下继续追问的接口。

## 自测清单

- [ ] 能否不看资料，用 30 秒说出核心结论？
- [ ] 能否写出/解释本题最关键的公式、shape 或信息流？
- [ ] 能否主动指出一个失效条件或反例？
- [ ] 能否把答案迁移到自己的一个项目 failure case？
- [ ] 我能独立回答追问：为什么 t 不等于简单相机世界坐标？
- [ ] 我能独立回答追问：PnP 在求什么？

## 关联题

- [Q077 · Optical Flow 是什么？](q077.md)
- [Q079 · 为什么 monocular depth 存在尺度歧义？](q079.md)
- [Q080 · BEV 为什么在自动驾驶视觉中常见？](q080.md)

## 资料来源

本题主体结构来自配套 PDF；公开依据与经典论文统一见 [`references/papers.md`](../12-references/papers.md)。

---

[← Q077](q077.md) [章节首页](README.md) [Q079 →](q079.md)
