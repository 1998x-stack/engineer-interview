---
id: Q024
title: "RRT 与 RRT* 有什么区别？为什么高维机械臂喜欢采样规划？"
chapter: "第三篇 规划、控制与 Manipulation"
difficulty: 4
tags: [control, manipulation, motion-planning, planning]
source: "PDF Core + Repository Expansion"
edition: "2026.09-v2"
---

# Q024 · RRT 与 RRT* 有什么区别？为什么高维机械臂喜欢采样规划？

[← 上一题](Q023.md) · [题库总索引](../../index.md#100-题总索引) · [下一题 →](Q025.md)

> **难度**：★★★★☆  
> **模块**：第三篇 规划、控制与 Manipulation  
> **标签**：`control` / `manipulation` / `motion-planning` / `planning`

> [!NOTE]
> **内容边界**：`PDF Core` 小节忠实保留 PDF 题解的术语、结构与结论；标记为 **Repository Expansion** 的内容是为了 GitHub 版进一步补充的标准理论、工程实践与 2026 资料。

## 1. 这道题在考什么？ — PDF Core

考察 configuration-space 规划。

### 为什么重要 — Repository Expansion

规划与控制题考察“从目标到安全轨迹”的完整链路。越接近实机，稳定性、碰撞、接触、控制频率和执行器约束越重要。

这题建议始终放回具身闭环理解：**Observation → State/Representation → Policy/Planner → Action → Controller → Robot → New Observation**。面试官通常会从定义继续追到“如果真的上机器人，接口、误差和失败模式是什么”。

## 2. 30 秒回答 — PDF Core

> RRT 快速随机探索可行空间；RRT* 增加邻域重连，具有渐近最优性。高维机械臂难以规则栅格化，sampling-based planning 更有优势。

## 3. 5-10 分钟深度题解

### 3.1 RRT 更强调快速找到可行解，RRT* 随样本增加持续改善路径

RRT 更强调快速找到可行解，RRT* 随样本增加持续改善路径。


### 3.2 性能取决于 collision checking、sampling distribution、steer/local planner

性能取决于 collision checking、sampling distribution、steer/local planner。


### 3.3 真实系统常先求可行路径，再做 shortcut / smoothing / time parameterization

真实系统常先求可行路径，再做 shortcut / smoothing / time parameterization。


### 3.X 专题增强 — Repository Expansion

- RRT* 的 rewiring 半径随样本数变化以保证渐近最优。高维机械臂规划的主要瓶颈往往是 collision checking，因此“采样算法本身”的复杂度不是全部。

## 4. 数学 / 公式 / 结构 — PDF Core + Expansion

```text
P(RRT* solution→optimum)→1 as N→∞（满足假设时）
```

面试白板上写出公式后，不要停在符号层。至少解释：**变量属于哪个空间、维度是什么、哪个量可观测、哪个量需要估计/优化，以及公式在接近边界条件时是否稳定**。

## 5. 白板实现应该写到什么程度？ — Repository Expansion

不要求现场实现完整工业系统，但应能写清**状态表示、主要循环、关键约束和失败回退**。若涉及学习算法，还要能指出训练 loop 与 rollout/evaluation loop 的区别。

## 6. 工程实现与系统接口 — Repository Expansion

把规划层和控制层职责拆开：规划产生可行路径/参考，控制保证高频跟踪与接触稳定；VLA/策略通常位于更上层，不应承担所有实时稳定性职责。

### 建议关注的指标

- 路径长度与规划耗时

- 最小障碍距离 / 碰撞率

- 跟踪误差 / 超调 / 稳态误差

- 接触力峰值与任务成功率

### 最小验证实验

1. 先构造一个可控、可复现的最小场景作为 baseline。
2. 只改变一个变量做 A/B；记录均值之外的失败分布。
3. 对边界条件或 OOD 场景单独建立回归测试。
4. 最后才进入全系统实机验证，并保留日志、视频和时序数据以便复盘。

## 7. 工程 Gotchas — PDF Core

- ⚠️ 说 RRT*“一定找到全局最优”而忽略有限时间。

### Debug Checklist — Repository Expansion

- [ ] 把失败分成“无可行路径”“轨迹不平滑”“控制跟不上”“模型目标错误”。

- [ ] 优先检查频率、限幅和坐标系。

- [ ] 接触任务必须同时看位置与力。

- [ ] 检查 train / validation / deployment 的 observation、action、单位、频率和 normalization 是否完全一致。

## 8. 连环追问

1. **PRM 与 RRT 如何选？**
   - 回答方向：先给机制结论，再给一个公式/系统边界，最后说明实机验证方法。

2. **goal bias 有什么作用？**
   - 回答方向：先给机制结论，再给一个公式/系统边界，最后说明实机验证方法。

## 9. 项目迁移题 — PDF Core

> 机械臂规划回答要提 collision geometry 和 joint constraints。

### 推荐项目回答模板 — Repository Expansion

```text
Problem / SLO
  → Baseline（必须有数字）
  → Failure taxonomy（不是“效果不好”）
  → Root-cause evidence
  → Intervention / ablation
  → Result（success / latency / safety / data efficiency）
  → Trade-off / remaining failure
```

## 10. 面试评分点

| 层级 | 表现 |
|---|---|
| 及格 | 能给出定义和核心结论 |
| 良好 | 能写公式/系统结构并解释边界条件 |
| 优秀 | 能讨论失败模式、工程指标和验证实验 |
| Senior/Research | 能比较替代方案，并从数据、成本、泛化和安全角度做取舍 |

## 11. 相关题目

- [Q025 · MoveIt 的典型 motion planning pipeline 是什么？](Q025.md)

- [Q023 · Costmap 中 inflation layer 的作用是什么？](Q023.md)

- [Q030 · 为什么端到端策略仍常配 classical low-level controller？](Q030.md)

- [Q029 · Force Control 与 Impedance Control 有什么区别？](Q029.md)

## 12. References / Further Reading

- **R18** · [MoveIt 2 Documentation](https://moveit.picknik.ai/)


## 13. 深度增强题解 — Repository Expansion v2.0

> 本节是 GitHub 版二次研究扩展，不替代前面的 PDF Core。目标是把“知道概念”提升到“能推导、能实现、能定位实机失败”。

### 13.1 从定义升级到问题建模

RRT 的优势是快速探索高维 free space；RRT* 在加入新节点后对邻域重连，随着样本增加渐近最优。高维机械臂很难显式栅格化 C-space，所以采样方法自然。

真实 motion planning 还需 collision checker 成本控制、goal bias、shortcut/smoothing。高 DoF 时“找到路径”与“可执行平滑轨迹”是两个问题。

面试时建议主动定义四件事：**输入是什么、输出是什么、变量属于哪个空间/时间尺度、成功由什么指标判定**。这会显著减少后续追问中的歧义。

### 13.2 Why：这个机制为什么有效？

1. **先找瓶颈/约束**：本题的方法不是孤立技巧，而是在特定假设下消除一个明确瓶颈。
2. **再找替代方案**：至少能说出一种替代路线，并说明它在数据、计算、稳定性、实时性或泛化上的取舍。
3. **最后落到闭环**：真正机器人系统最终都会回到 `感知 → 状态 → 决策 → 动作 → 控制 → 新观测`；任何局部优化都要验证是否提高闭环 task metric。

一个常用的工程化表达是：

```text
离线指标改善
   ↓（必要但不充分）
模块级接口正确
   ↓
闭环 rollout 稳定
   ↓
OOD / 长尾仍成立
   ↓
实机 SLO / 安全约束满足
```

### 13.3 白板 / 实现骨架

```text
Goal → Planning Scene/State → Global/Task Planner
     → Feasible trajectory → Time parameterization
     → Low-level controller → actuator
     → safety monitor / fallback
```
规划成功不等于执行成功；必须分别观测 planning、execution、control 三层。

**白板必画**：画 planner→trajectory→controller→robot→sensor feedback，并标出 safety/fallback 插入位置。

### 13.4 失败模式：不要只会解释“成功时为什么工作”

| 失败类型 | 典型现象 | 优先检查的证据 |
|---|---|---|
| F1 | 规划成功但轨迹不可执行 | 最小可复现实验 + 原始时序日志 |
| F2 | 控制频率/延迟导致闭环失稳 | 边界条件 / OOD 分桶统计 |
| F3 | 接触模型或 force sensor 误差 | 模块输入输出与数值范围 |
| F4 | 安全约束只在规划层而非执行层 | 闭环回放、视频、控制/系统 telemetry |

专业回答应避免“模型效果不好”这种不可诊断描述，而是把失败拆成可观测类别，并给每一类一个证据链。

### 13.5 指标体系：怎样证明你的答案在工程上成立？

本题至少建议关注：**plan success、planning time、clearance、trajectory jerk、tracking error、contact force、collision rate**。

评估时优先使用：

- **均值 + 分位数/置信区间**，避免只报单个平均数；
- **按场景分桶**：seen / OOD、短时 / 长时、成功 / near-miss；
- **A/B 单变量实验**：一次只改一个核心变量；
- **闭环指标优先于离线 proxy**：离线 loss/accuracy 只能解释部分问题。

### 13.6 面试追问树：从“会答”到“扛得住追问”

- **PRM 与 RRT 如何选？**：先给 1 句结论；再回到本题的核心变量/公式；最后给一个可测的实机或离线证据。不要只复述名词。
- **goal bias 有什么作用？**：先给 1 句结论；再回到本题的核心变量/公式；最后给一个可测的实机或离线证据。不要只复述名词。

进一步的 Senior 追问通常会变成：

1. **如果这个方法不起作用，你如何判断是数据、模型、控制还是系统问题？**
2. **如果算力减半 / 数据减半 / 延迟预算减半，你会保留什么？**
3. **如何设计最小 ablation 证明改进来自你声称的机制，而不是额外参数量或更多数据？**
4. **这个结论在哪些 robot / task / control regime 下可能失效？**

### 13.7 项目化回答模板

```text
Context
  → Robot / Task / Sensors / Control rate
Problem
  → 用一个可测失败指标定义问题
Baseline
  → 给出至少一个数字
Evidence
  → 日志 / 回放 / profiling / ablation
Intervention
  → 只修改关键变量
Result
  → success + latency/safety/data-efficiency
Trade-off
  → 说明代价和剩余失败
Next iteration
  → 下一批数据或系统改进如何选
```

### 13.8 一页速记

- **一句话本质**：RRT 的优势是快速探索高维 free space；RRT* 在加入新节点后对邻域重连，随着样本增加渐近最优。
- **最容易答错**：只背术语，不定义 frame / state / action / time scale / metric。
- **工程优先级**：先验证输入输出和时序，再优化模型；先建立 failure taxonomy，再增加数据/参数。
- **面试高分信号**：能给公式或结构图 + 能说替代方案 + 能给失败证据 + 能设计 A/B。

---

**记忆锚点**：先说机制，再说边界；先画闭环，再谈模型。
