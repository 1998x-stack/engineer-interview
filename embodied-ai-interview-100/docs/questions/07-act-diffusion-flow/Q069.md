---
id: Q069
title: "Receding Horizon Control 为什么和生成式动作策略很搭？"
chapter: "第七篇 ACT / Diffusion / Flow Matching"
difficulty: 4
tags: [act, action-modeling, diffusion-policy, flow-matching]
source: "PDF Core + Repository Expansion"
edition: "2026.09-v2"
---

# Q069 · Receding Horizon Control 为什么和生成式动作策略很搭？

[← 上一题](Q068.md) · [题库总索引](../../index.md#100-题总索引) · [下一题 →](Q070.md)

> **难度**：★★★★☆  
> **模块**：第七篇 ACT / Diffusion / Flow Matching  
> **标签**：`act` / `action-modeling` / `diffusion-policy` / `flow-matching`

> [!NOTE]
> **内容边界**：`PDF Core` 小节忠实保留 PDF 题解的术语、结构与结论；标记为 **Repository Expansion** 的内容是为了 GitHub 版进一步补充的标准理论、工程实践与 2026 资料。

## 1. 这道题在考什么？ — PDF Core

考察闭环执行思想。

### 为什么重要 — Repository Expansion

现代动作生成从单步回归走向 chunk 和生成式建模，核心原因是时序一致性与多模态动作分布。面试必须能解释“为什么 MSE 会平均模式、为什么 chunk 有闭环风险、为什么生成模型会引入延迟”。

这题建议始终放回具身闭环理解：**Observation → State/Representation → Policy/Planner → Action → Controller → Robot → New Observation**。面试官通常会从定义继续追到“如果真的上机器人，接口、误差和失败模式是什么”。

## 2. 30 秒回答 — PDF Core

> 模型每次预测未来 H 步，但只执行前 k<H 步，再用新观测重新规划，使 trajectory-level coherence 与 closed-loop correction 同时存在。

## 3. 5-10 分钟深度题解

### 3.1 这是 MPC 的基本思想，也被 Diffusion Policy 等采用

这是 MPC 的基本思想，也被 Diffusion Policy 等采用。


### 3.2 k 过大更像 open-loop；k 太小增加 inference burden

k 过大更像 open-loop；k 太小增加 inference burden。


### 3.3 异步 policy 可在当前 chunk 运行时生成下一 chunk，但要处理 observation delay

异步 policy 可在当前 chunk 运行时生成下一 chunk，但要处理 observation delay。


## 4. 数学 / 公式 / 结构 — PDF Core + Expansion

```text
predict H → execute k → observe → replan
```

面试白板上写出公式后，不要停在符号层。至少解释：**变量属于哪个空间、维度是什么、哪个量可观测、哪个量需要估计/优化，以及公式在接近边界条件时是否稳定**。

## 5. 白板实现应该写到什么程度？ — Repository Expansion

不要求现场实现完整工业系统，但应能写清**状态表示、主要循环、关键约束和失败回退**。若涉及学习算法，还要能指出训练 loop 与 rollout/evaluation loop 的区别。

## 6. 工程实现与系统接口 — Repository Expansion

工程上重点权衡 chunk horizon、replan frequency、sampling/ODE steps、action smoothing、异步推理和低层控制。生成质量必须和 control rate 一起看。

### 建议关注的指标

- action prediction NLL/denoising loss

- rollout success

- 推理延迟与有效控制频率

- 轨迹平滑度 / jerk / recovery rate

### 最小验证实验

1. 先构造一个可控、可复现的最小场景作为 baseline。
2. 只改变一个变量做 A/B；记录均值之外的失败分布。
3. 对边界条件或 OOD 场景单独建立回归测试。
4. 最后才进入全系统实机验证，并保留日志、视频和时序数据以便复盘。

## 7. 工程 Gotchas — PDF Core

- ⚠️ 把 action chunk 全部执行完再看反馈。

### Debug Checklist — Repository Expansion

- [ ] 若动作抖动先检查 chunk overlap 和时间对齐。

- [ ] 若延迟过高分析采样步数和视觉 backbone。

- [ ] 若生成轨迹多样但成功率低，检查条件信息是否充分。

- [ ] 检查 train / validation / deployment 的 observation、action、单位、频率和 normalization 是否完全一致。

## 8. 连环追问

1. **k 如何选？**
   - 回答方向：先给机制结论，再给一个公式/系统边界，最后说明实机验证方法。

2. **新旧 chunk 如何平滑切换？**
   - 回答方向：先给机制结论，再给一个公式/系统边界，最后说明实机验证方法。

## 9. 项目迁移题 — PDF Core

> 项目中描述 chunk overlap、interpolation 与 emergency stop。

### 推荐项目回答模板 — Repository Expansion

```text
Problem / SLO
  → Baseline（必须有数字）
  → Failure taxonomy（不是“效果不好”）
  → Root-cause evidence
  → Intervention / ablation
  → Result（success / latency / safety / data efficiency）
  → Trade-off / remaining failure
```

## 10. 面试评分点

| 层级 | 表现 |
|---|---|
| 及格 | 能给出定义和核心结论 |
| 良好 | 能写公式/系统结构并解释边界条件 |
| 优秀 | 能讨论失败模式、工程指标和验证实验 |
| Senior/Research | 能比较替代方案，并从数据、成本、泛化和安全角度做取舍 |

## 11. 相关题目

- [Q070 · ACT、Diffusion、Flow Matching 三者应该怎么选？](Q070.md)

- [Q068 · Action Chunk 为什么不能无限长？](Q068.md)

- [Q067 · Flow Matching 与 Diffusion 有什么区别？](Q067.md)

- [Q066 · Diffusion Policy 最大的部署瓶颈是什么？如何加速？](Q066.md)

## 12. References / Further Reading

- **R3** · [Diffusion Policy: Visuomotor Policy Learning via Action Diffusion](https://arxiv.org/abs/2303.04137)


## 13. 深度增强题解 — Repository Expansion v2.0

> 本节是 GitHub 版二次研究扩展，不替代前面的 PDF Core。目标是把“知道概念”提升到“能推导、能实现、能定位实机失败”。

### 13.1 从定义升级到问题建模

Receding horizon 每次预测 H 步但只执行前 k 步，然后重新观测并重规划，兼顾轨迹一致性与反馈校正。它把 generative action sequence 从 open-loop 变成 closed-loop。

k 的选择是 feedback bandwidth：k 越小越灵敏，但推理负载更高；k 越大吞吐更好，但更容易错过环境变化。

面试时建议主动定义四件事：**输入是什么、输出是什么、变量属于哪个空间/时间尺度、成功由什么指标判定**。这会显著减少后续追问中的歧义。

### 13.2 Why：这个机制为什么有效？

1. **先找瓶颈/约束**：本题的方法不是孤立技巧，而是在特定假设下消除一个明确瓶颈。
2. **再找替代方案**：至少能说出一种替代路线，并说明它在数据、计算、稳定性、实时性或泛化上的取舍。
3. **最后落到闭环**：真正机器人系统最终都会回到 `感知 → 状态 → 决策 → 动作 → 控制 → 新观测`；任何局部优化都要验证是否提高闭环 task metric。

一个常用的工程化表达是：

```text
离线指标改善
   ↓（必要但不充分）
模块级接口正确
   ↓
闭环 rollout 稳定
   ↓
OOD / 长尾仍成立
   ↓
实机 SLO / 安全约束满足
```

### 13.3 白板 / 实现骨架

```python
# Generative action policy 统一抽象
context = encode(obs_history, proprio, language)
action_seq = generator.sample(context, horizon=H, steps=N)
execute(action_seq[:K])           # K < H
obs = sense_again()               # receding horizon
```
比较不同生成器时，必须固定 H、K、backbone 和数据。

**白板必画**：画 action distribution 从 data/noise 到 chunk 的生成过程，再画 receding horizon 的 execute K / predict H。

### 13.4 失败模式：不要只会解释“成功时为什么工作”

| 失败类型 | 典型现象 | 优先检查的证据 |
|---|---|---|
| F1 | 生成动作平滑但错误模式被持续执行 | 最小可复现实验 + 原始时序日志 |
| F2 | 采样步数太少导致精度下降 | 边界条件 / OOD 分桶统计 |
| F3 | chunk 太长导致反馈滞后 | 模块输入输出与数值范围 |
| F4 | 训练/部署 horizon 或 normalization 不一致 | 闭环回放、视频、控制/系统 telemetry |

专业回答应避免“模型效果不好”这种不可诊断描述，而是把失败拆成可观测类别，并给每一类一个证据链。

### 13.5 指标体系：怎样证明你的答案在工程上成立？

本题至少建议关注：**success、action NLL/denoising loss（辅助）、inference steps/ms、jerk、horizon sensitivity、mode coverage**。

评估时优先使用：

- **均值 + 分位数/置信区间**，避免只报单个平均数；
- **按场景分桶**：seen / OOD、短时 / 长时、成功 / near-miss；
- **A/B 单变量实验**：一次只改一个核心变量；
- **闭环指标优先于离线 proxy**：离线 loss/accuracy 只能解释部分问题。

### 13.6 面试追问树：从“会答”到“扛得住追问”

- **k 如何选？**：先给 1 句结论；再回到本题的核心变量/公式；最后给一个可测的实机或离线证据。不要只复述名词。
- **新旧 chunk 如何平滑切换？**：先给 1 句结论；再回到本题的核心变量/公式；最后给一个可测的实机或离线证据。不要只复述名词。

进一步的 Senior 追问通常会变成：

1. **如果这个方法不起作用，你如何判断是数据、模型、控制还是系统问题？**
2. **如果算力减半 / 数据减半 / 延迟预算减半，你会保留什么？**
3. **如何设计最小 ablation 证明改进来自你声称的机制，而不是额外参数量或更多数据？**
4. **这个结论在哪些 robot / task / control regime 下可能失效？**

### 13.7 项目化回答模板

```text
Context
  → Robot / Task / Sensors / Control rate
Problem
  → 用一个可测失败指标定义问题
Baseline
  → 给出至少一个数字
Evidence
  → 日志 / 回放 / profiling / ablation
Intervention
  → 只修改关键变量
Result
  → success + latency/safety/data-efficiency
Trade-off
  → 说明代价和剩余失败
Next iteration
  → 下一批数据或系统改进如何选
```

### 13.8 一页速记

- **一句话本质**：Receding horizon 每次预测 H 步但只执行前 k 步，然后重新观测并重规划，兼顾轨迹一致性与反馈校正。
- **最容易答错**：只背术语，不定义 frame / state / action / time scale / metric。
- **工程优先级**：先验证输入输出和时序，再优化模型；先建立 failure taxonomy，再增加数据/参数。
- **面试高分信号**：能给公式或结构图 + 能说替代方案 + 能给失败证据 + 能设计 A/B。

---

**记忆锚点**：先说机制，再说边界；先画闭环，再谈模型。
