---
id: Q020
title: "Synthetic Data 在预训练阶段的价值与风险是什么？"
chapter: "Tokenizer 与预训练数据"
chapter_id: 02
difficulty: "中"
frequency: "★★★★★"
source_type: "B 类等价追问题"
tags:
  - data
  - tokenizer-data
---

# Q020. Synthetic Data 在预训练阶段的价值与风险是什么？

> **定位**：B 类等价追问题 · 难度 **中** · 频率 **★★★★★**
> 章节：[Tokenizer 与预训练数据](./README.md)

## 1. 面试官为什么问

合成数据已进入主流模型训练，需要理解信息增益而非“越多越好”。

这道题真正区分的是：**能否把“知道概念”推进到“解释机制、量化成本、落到训练系统并设计验证”**。

### 本题的三层观察镜

- **分布**：每个过滤/采样动作怎样改变 p_train(x)？
- **信息密度**：单位 Token 的新颖性、正确性和覆盖度怎样衡量？
- **可追溯**：能否从训练异常追到具体 shard/domain/sample？

## 2. 30 秒结论

> 合成数据真正要最大化的是“每个 token 带来的新、对、难的信息”。

30 秒版本的目标不是覆盖所有细节，而是先给出**正确结论 + 最关键因果关系**，让面试官知道你掌握主线。

## 3. 3 分钟标准回答

### 3.1 核心机制

Synthetic data 可以补足数学、代码、推理、稀缺语言等高价值域，控制难度和格式，并通过程序/规则做 correctness filtering。Qwen3 官方资料也描述了利用模型生成数学和代码数据。

### 3.2 关键 Trade-off / 边界

风险包括 teacher bias、错误事实放大、风格同质化、真实分布覆盖下降以及递归训练导致的信息退化。核心指标应是 information gain per token，而不是合成 token 数。

## 4. 深度展开：从定义走向 Production

本题的深挖重点是：**合成数据真正要最大化的是“每个 token 带来的新、对、难的信息”。**

建议沿下面的因果链继续展开：

1. **定义/状态语义**：先把对象、tensor、数据分布或训练状态说准确；
2. **机制**：解释为什么该设计会改变 loss、memory、FLOPs、通信或稳定性；
3. **代价**：指出它牺牲了什么，不把工程 trick 说成“免费午餐”；
4. **可验证性**：说明你会用什么指标、profile 或 ablation 判断结论是否成立；
5. **规模化边界**：把 1 GPU/小模型结论推进到千卡、长序列或 MoE 场景。

## 5. Know-how：如何落到实现与训练系统

1. **动作 1**：给生成器、过滤器与最终训练模型分离版本，避免循环自证。
2. **动作 2**：使用可验证任务优先：代码执行、数学答案、结构约束。
3. **动作 3**：混入真实数据并做 diversity/novelty 监控。

### 数据闭环

从样本级 provenance 到 domain-level held-out loss 建立闭环：数据变更必须能追溯到采样率、重复率、污染率和能力变化。

- 所有过滤规则可版本化。
- Mixture 调整用 proxy run 或受控消融。
- benchmark contamination 与训练质量评估分开。

## 6. 高频追问与回答方向

### 追问 1：如何防止 teacher 的错误被学生放大？

**回答方向**：给出可执行步骤，并补充至少一个可观测指标和一个对照实验，避免只给概念。

### 追问 2：为什么 rejection sampling 仍可能降低多样性？

**回答方向**：从“旧方案瓶颈 → 合成数据真正要最大化的是“每个 token 带来的新、对、难的信息”。 → 新代价/边界条件”三段式回答。

### 追问 3：怎样衡量 synthetic data 的边际价值？

**回答方向**：回到定义、数学/状态语义、工程代价和可验证实验四层作答。

## 7. 常见失分点：为什么错

- ❌ **把合成数据等同于 SFT 数据。**
  - 改进：回到“合成数据真正要最大化的是“每个 token 带来的新、对、难的信息”。”这一主线，并补充可量化的状态/公式/实验，而不是只给结论。
- ❌ **只看过滤通过率，不测模型增益。**
  - 改进：回到“合成数据真正要最大化的是“每个 token 带来的新、对、难的信息”。”这一主线，并补充可量化的状态/公式/实验，而不是只给结论。

## 8. 面试评分 Rubric

| 层级 | 表现 |
|---|---|
| 0 — 不合格 | 只会名词或结论，定义/公式/状态语义存在明显错误。 |
| 1 — 基础 | 能复述：合成数据真正要最大化的是“每个 token 带来的新、对、难的信息”。 |
| 2 — 良好 | 能解释 Why + How，回答至少一个追问，并指出主要 trade-off。 |
| 3 — 强 | 能手算/画通信或数据流/给出实验设计，把算法与系统联系起来。 |
| 4 — 研究/Production | 能说明失效边界、诊断指标、等成本对照和规模化约束，并结合真实项目给数字。 |

## 9. 自测与白板练习

闭卷完成下面练习，建议每题控制在 5–10 分钟：

1. 如何防止 teacher 的错误被学生放大？
2. 为什么 rejection sampling 仍可能降低多样性？
3. 怎样衡量 synthetic data 的边际价值？
4. 设计一个最小数据消融，说明你会记录哪些 provenance 与 domain metrics。

## 10. 一句话记忆

> **合成数据真正要最大化的是“每个 token 带来的新、对、难的信息”。**

## 11. 题源与可信度说明

基于预训练岗位能力模型、技术报告与高频追问扩展出的等价深挖题；不宣称是某家公司逐字原题。

> 本项目将“公开面经题型”和“能力等价追问题”明确分开；公开面经用于观察招聘分布，不视为公司官方试卷。

## 12. 延伸阅读

- **R11 · 技术报告** — [Qwen Team, Qwen3 Technical Report](https://arxiv.org/abs/2505.09388)
- **R12 · 官方博客** — [Qwen3 Official Blog](https://qwenlm.github.io/blog/qwen3/)

## 相关题目

- ← [Q019. Packing 与 Padding 有什么区别？为什么 Packing 不是简单拼接？](./019.md)

---

> **复习要求**：能够在不看答案的情况下完成 30 秒结论、3 分钟主答，并承受至少 3 轮 Why/How/Trade-off/Production 追问。
