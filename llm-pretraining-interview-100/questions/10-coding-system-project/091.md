---
id: Q091
title: "手写 Multi-Head Attention，面试官真正看什么？"
chapter: "手撕、系统设计与项目拷打"
chapter_id: 10
difficulty: "中"
frequency: "★★★★★"
source_type: "A 类高频真题型"
tags:
  - attention
  - coding
  - coding-system-project
---

# Q091. 手写 Multi-Head Attention，面试官真正看什么？

> **定位**：A 类高频真题型 · 难度 **中** · 频率 **★★★★★**
> 章节：[手撕、系统设计与项目拷打](./README.md)

## 1. 面试官为什么问

公开面经高频手撕题，shape 正确比 API 熟练更重要。

这道题真正区分的是：**能否把“知道概念”推进到“解释机制、量化成本、落到训练系统并设计验证”**。

### 本题的三层观察镜

- **正确性**：代码/公式/系统是否在边界条件下仍正确？
- **量化**：参数、显存、吞吐、成本是否可手算/实测？
- **证明**：项目贡献是否有对照实验和可归因结果？

## 2. 30 秒结论

> 手撕 Attention 的第一原则：每一行代码旁边都知道 tensor shape。

30 秒版本的目标不是覆盖所有细节，而是先给出**正确结论 + 最关键因果关系**，让面试官知道你掌握主线。

## 3. 3 分钟标准回答

### 3.1 核心机制

输入 [B,S,D] 经过 QKV projection，reshape/transposition 为 [B,H,S,D_h]；计算 score=[B,H,S,S]，除 √D_h，加入 causal/padding mask，softmax 后与 V 相乘，再 transpose/reshape 回 [B,S,D] 并做 output projection。

### 3.2 关键 Trade-off / 边界

面试官通常观察你是否能持续维护 shape、softmax 维度、mask 语义和 contiguous/view 问题，而不是能否背 torch.nn.MultiheadAttention。

### 核心公式

```text
[B,S,D] → [B,H,S,D_h] → scores [B,H,S,S] → [B,S,D]
```

> 面试时不要只写公式。要逐个解释符号、shape/量纲，以及公式如何导出工程结论。

## 4. 深度展开

### PyTorch 参考骨架
```python
class MHA(nn.Module):
    def forward(self, x, mask=None):
        B, S, D = x.shape
        qkv = self.qkv(x).view(B, S, 3, self.h, self.dh)
        q, k, v = qkv.unbind(dim=2)
        q, k, v = [t.transpose(1, 2) for t in (q, k, v)]  # [B,H,S,Dh]
        score = q @ k.transpose(-2, -1) / math.sqrt(self.dh)
        if mask is not None:
            score = score.masked_fill(~mask, float('-inf'))
        p = score.softmax(dim=-1)
        y = (p @ v).transpose(1, 2).contiguous().view(B, S, D)
        return self.out(y)
```
面试时必须同步说出每一步 shape、mask 广播规则和数值稳定性。

## 5. Know-how：如何落到实现与训练系统

1. **动作 1**：先在白板逐行标 shape，再写代码。
2. **动作 2**：mask 使用足够负值并考虑 dtype。
3. **动作 3**：为 B=2,S=4,H=2 写最小单元测试。

### 白板到 Production

要求答案同时包含正确性、复杂度、测试、边界条件、工程约束和量化结果。

- 代码题写 shape/assert/unit test。
- 设计题先声明约束。
- 项目题必须给数字和对照。

## 6. 高频追问与回答方向

### 追问 1：padding mask 与 causal mask 怎么合并？

**回答方向**：给出可执行步骤，并补充至少一个可观测指标和一个对照实验，避免只给概念。

### 追问 2：GQA 如何改代码？

**回答方向**：给出可执行步骤，并补充至少一个可观测指标和一个对照实验，避免只给概念。

### 追问 3：KV Cache 版 decode 如何写？

**回答方向**：先声明 dtype 与 batch/seq/layers/heads 等变量，再拆 weights、states、activation、buffer；最后说明分片轴。

## 7. 常见失分点：为什么错

- ❌ **softmax dim 写错。**
  - 改进：回到“手撕 Attention 的第一原则：每一行代码旁边都知道 tensor shape。”这一主线，并补充可量化的状态/公式/实验，而不是只给结论。
- ❌ **reshape 前后 head/sequence 维混乱。**
  - 改进：回到“手撕 Attention 的第一原则：每一行代码旁边都知道 tensor shape。”这一主线，并补充可量化的状态/公式/实验，而不是只给结论。

## 8. 面试评分 Rubric

| 层级 | 表现 |
|---|---|
| 0 — 不合格 | 只会名词或结论，定义/公式/状态语义存在明显错误。 |
| 1 — 基础 | 能复述：手撕 Attention 的第一原则：每一行代码旁边都知道 tensor shape。 |
| 2 — 良好 | 能解释 Why + How，回答至少一个追问，并指出主要 trade-off。 |
| 3 — 强 | 能手算/画通信或数据流/给出实验设计，把算法与系统联系起来。 |
| 4 — 研究/Production | 能说明失效边界、诊断指标、等成本对照和规模化约束，并结合真实项目给数字。 |

## 9. 自测与白板练习

闭卷完成下面练习，建议每题控制在 5–10 分钟：

1. padding mask 与 causal mask 怎么合并？
2. GQA 如何改代码？
3. KV Cache 版 decode 如何写？
4. 把答案写成可运行代码/可核算公式/可量化项目复盘之一。

## 10. 一句话记忆

> **手撕 Attention 的第一原则：每一行代码旁边都知道 tensor shape。**

## 11. 题源与可信度说明

公开面经中出现或高度近似的高频题型；题干在本项目中做了规范化重写。

> 本项目将“公开面经题型”和“能力等价追问题”明确分开；公开面经用于观察招聘分布，不视为公司官方试卷。

## 12. 延伸阅读

- **R13 · 公开面经** — [公开面经：字节大模型算法岗](https://www.nowcoder.com/discuss/922308546966847488)
- **R16 · 公开面经** — [公开面经：爱奇艺大模型算法岗](https://www.nowcoder.com/feed/main/detail/f35c2be602e44be09637c974102c11f3)

## 相关题目

- → [Q092. 手写 Causal Mask：如何保证语义和数值都正确？](./092.md)

---

> **复习要求**：能够在不看答案的情况下完成 30 秒结论、3 分钟主答，并承受至少 3 轮 Why/How/Trade-off/Production 追问。
