---
id: Q092
title: "手写 Causal Mask：如何保证语义和数值都正确？"
chapter: "手撕、系统设计与项目拷打"
chapter_id: 10
difficulty: "易"
frequency: "★★★★☆"
source_type: "B 类等价追问题"
tags:
  - attention
  - coding
  - coding-system-project
---

# Q092. 手写 Causal Mask：如何保证语义和数值都正确？

> **定位**：B 类等价追问题 · 难度 **易** · 频率 **★★★★☆**
> 章节：[手撕、系统设计与项目拷打](./README.md)

## 1. 面试官为什么问

小题但能暴露 off-by-one 与 dtype 错误。

这道题真正区分的是：**能否把“知道概念”推进到“解释机制、量化成本、落到训练系统并设计验证”**。

### 本题的三层观察镜

- **正确性**：代码/公式/系统是否在边界条件下仍正确？
- **量化**：参数、显存、吞吐、成本是否可手算/实测？
- **证明**：项目贡献是否有对照实验和可归因结果？

## 2. 30 秒结论

> Causal mask 不是“把未来概率清零”，而是让未来 logit 在 softmax 前不可竞争。

30 秒版本的目标不是覆盖所有细节，而是先给出**正确结论 + 最关键因果关系**，让面试官知道你掌握主线。

## 3. 3 分钟标准回答

### 3.1 核心机制

对 query 位置 i，只允许访问 key 位置 j≤i；未来位置加 -∞（或 dtype 安全的极小值）后再 softmax。若包含 padding，还要把 padding key 同时屏蔽。

### 3.2 关键 Trade-off / 边界

在 packed sequence 中，单纯下三角 mask 可能不够：如果不允许跨文档 attention，还要加入 document boundary mask。

### 核心公式

```text
M_ij=0 if j≤i else -∞
```

> 面试时不要只写公式。要逐个解释符号、shape/量纲，以及公式如何导出工程结论。

## 4. 深度展开

### Mask 的两个不变量
1. 语义：位置 $i$ 不能看到未来位置 $j>i$；
2. 数值：被 mask 的 logit 在 softmax 后应为 0，且不能因为 dtype/全 mask row 产生 NaN。

真实训练还要考虑 padding/document boundary 与 causal mask 的组合。

## 5. Know-how：如何落到实现与训练系统

1. **动作 1**：用 4×4 手工矩阵做 unit test。
2. **动作 2**：检查 diagonal 是否可见、padding query 的 loss 是否 mask。
3. **动作 3**：低精度下避免不安全常数导致 overflow/NaN。

### 白板到 Production

要求答案同时包含正确性、复杂度、测试、边界条件、工程约束和量化结果。

- 代码题写 shape/assert/unit test。
- 设计题先声明约束。
- 项目题必须给数字和对照。

## 6. 高频追问与回答方向

### 追问 1：为什么 mask 加在 softmax 前？

**回答方向**：从“旧方案瓶颈 → Causal mask 不是“把未来概率清零”，而是让未来 logit 在 softmax 前不可竞争。 → 新代价/边界条件”三段式回答。

### 追问 2：packed docs 如何 block diagonal？

**回答方向**：给出可执行步骤，并补充至少一个可观测指标和一个对照实验，避免只给概念。

### 追问 3：FlashAttention 的 causal flag 等价什么？

**回答方向**：回到定义、数学/状态语义、工程代价和可验证实验四层作答。

## 7. 常见失分点：为什么错

- ❌ **把未来位置设 0 再 softmax。**
  - 改进：回到“Causal mask 不是“把未来概率清零”，而是让未来 logit 在 softmax 前不可竞争。”这一主线，并补充可量化的状态/公式/实验，而不是只给结论。
- ❌ **搞反上三角/下三角。**
  - 改进：回到“Causal mask 不是“把未来概率清零”，而是让未来 logit 在 softmax 前不可竞争。”这一主线，并补充可量化的状态/公式/实验，而不是只给结论。

## 8. 面试评分 Rubric

| 层级 | 表现 |
|---|---|
| 0 — 不合格 | 只会名词或结论，定义/公式/状态语义存在明显错误。 |
| 1 — 基础 | 能复述：Causal mask 不是“把未来概率清零”，而是让未来 logit 在 softmax 前不可竞争。 |
| 2 — 良好 | 能解释 Why + How，回答至少一个追问，并指出主要 trade-off。 |
| 3 — 强 | 能手算/画通信或数据流/给出实验设计，把算法与系统联系起来。 |
| 4 — 研究/Production | 能说明失效边界、诊断指标、等成本对照和规模化约束，并结合真实项目给数字。 |

## 9. 自测与白板练习

闭卷完成下面练习，建议每题控制在 5–10 分钟：

1. 为什么 mask 加在 softmax 前？
2. packed docs 如何 block diagonal？
3. FlashAttention 的 causal flag 等价什么？
4. 把答案写成可运行代码/可核算公式/可量化项目复盘之一。

## 10. 一句话记忆

> **Causal mask 不是“把未来概率清零”，而是让未来 logit 在 softmax 前不可竞争。**

## 11. 题源与可信度说明

基于预训练岗位能力模型、技术报告与高频追问扩展出的等价深挖题；不宣称是某家公司逐字原题。

> 本项目将“公开面经题型”和“能力等价追问题”明确分开；公开面经用于观察招聘分布，不视为公司官方试卷。

## 12. 延伸阅读


## 相关题目

- ← [Q091. 手写 Multi-Head Attention，面试官真正看什么？](./091.md)
- → [Q093. 手写 RMSNorm：最常见错误是什么？](./093.md)

---

> **复习要求**：能够在不看答案的情况下完成 30 秒结论、3 分钟主答，并承受至少 3 轮 Why/How/Trade-off/Production 追问。
