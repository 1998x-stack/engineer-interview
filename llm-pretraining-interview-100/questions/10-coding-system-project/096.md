---
id: Q096
title: "手算 70B + Adam + BF16 的训练显存，并推到 ZeRO"
chapter: "手撕、系统设计与项目拷打"
chapter_id: 10
difficulty: "难"
frequency: "★★★★★"
source_type: "A 类高频真题型"
tags:
  - coding
  - coding-system-project
  - distributed
  - memory
  - optimization
  - precision
---

# Q096. 手算 70B + Adam + BF16 的训练显存，并推到 ZeRO

> **定位**：A 类高频真题型 · 难度 **难** · 频率 **★★★★★**
> 章节：[手撕、系统设计与项目拷打](./README.md)

## 1. 面试官为什么问

预训练岗位 S 级题：假设、公式和分片必须都对。

这道题真正区分的是：**能否把“知道概念”推进到“解释机制、量化成本、落到训练系统并设计验证”**。

### 本题的三层观察镜

- **正确性**：代码/公式/系统是否在边界条件下仍正确？
- **量化**：参数、显存、吞吐、成本是否可手算/实测？
- **证明**：项目贡献是否有对照实验和可归因结果？

## 2. 30 秒结论

> 显存手算模板：列状态 → 算 bytes → 标复制/分片 → 加 activation/峰值。

30 秒版本的目标不是覆盖所有细节，而是先给出**正确结论 + 最关键因果关系**，让面试官知道你掌握主线。

## 3. 3 分钟标准回答

### 3.1 核心机制

先声明一种常见假设：BF16 params 2B、BF16 grads 2B、FP32 master 4B、Adam m/v 各4B，总计 16 bytes/param。70B model states 约 1.12TB；这还不包括 activation、临时 buffer 和 fragmentation。

### 3.2 关键 Trade-off / 边界

然后按 ZeRO stage 分片：Stage1 只把 optimizer 部分按 DP 分；Stage2 再分 grad；Stage3 连 param 也按 DP 分。真正单卡峰值还要加入参数 all-gather、activation 和通信 workspace。

### 核心公式

```text
示例基线：70×10⁹ × 16 bytes ≈ 1.12 TB（model states，未分片）
```

> 面试时不要只写公式。要逐个解释符号、shape/量纲，以及公式如何导出工程结论。

## 4. 深度展开

### 70B 训练显存答题模板
1. 声明 weight/grad/master/m/v 的 dtype；
2. 按 bytes/param × 70B 算 model states；
3. 指出 DP replica；
4. 应用 ZeRO-1/2/3 或 FSDP 的分片因子；
5. 再估 activation、临时 gather buffer、通信 buffer 和 allocator peak。

面试官更看重**假设透明与分片语义正确**，不是背一个固定 TB 数。

## 5. Know-how：如何落到实现与训练系统

1. **动作 1**：白板用表格列 state、dtype、总字节、是否 /DP。
2. **动作 2**：给出“理论常驻”与“运行峰值”两个数字。
3. **动作 3**：说明不同框架没有 master copy 时数字会变化。

### 白板到 Production

要求答案同时包含正确性、复杂度、测试、边界条件、工程约束和量化结果。

- 代码题写 shape/assert/unit test。
- 设计题先声明约束。
- 项目题必须给数字和对照。

## 6. 高频追问与回答方向

### 追问 1：DP=64，ZeRO-2 常驻 model states 多少？

**回答方向**：回到定义、数学/状态语义、工程代价和可验证实验四层作答。

### 追问 2：Stage3 为什么峰值不等于总状态/DP？

**回答方向**：从“旧方案瓶颈 → 显存手算模板：列状态 → 算 bytes → 标复制/分片 → 加 activation/峰值。 → 新代价/边界条件”三段式回答。

### 追问 3：加 activation checkpointing 影响哪一项？

**回答方向**：回到定义、数学/状态语义、工程代价和可验证实验四层作答。

## 7. 常见失分点：为什么错

- ❌ **不声明 dtype 假设。**
  - 改进：回到“显存手算模板：列状态 → 算 bytes → 标复制/分片 → 加 activation/峰值。”这一主线，并补充可量化的状态/公式/实验，而不是只给结论。
- ❌ **把全部 16B/param 在 ZeRO-1 就除以 DP。**
  - 改进：回到“显存手算模板：列状态 → 算 bytes → 标复制/分片 → 加 activation/峰值。”这一主线，并补充可量化的状态/公式/实验，而不是只给结论。

## 8. 面试评分 Rubric

| 层级 | 表现 |
|---|---|
| 0 — 不合格 | 只会名词或结论，定义/公式/状态语义存在明显错误。 |
| 1 — 基础 | 能复述：显存手算模板：列状态 → 算 bytes → 标复制/分片 → 加 activation/峰值。 |
| 2 — 良好 | 能解释 Why + How，回答至少一个追问，并指出主要 trade-off。 |
| 3 — 强 | 能手算/画通信或数据流/给出实验设计，把算法与系统联系起来。 |
| 4 — 研究/Production | 能说明失效边界、诊断指标、等成本对照和规模化约束，并结合真实项目给数字。 |

## 9. 自测与白板练习

闭卷完成下面练习，建议每题控制在 5–10 分钟：

1. DP=64，ZeRO-2 常驻 model states 多少？
2. Stage3 为什么峰值不等于总状态/DP？
3. 加 activation checkpointing 影响哪一项？
4. 把答案写成可运行代码/可核算公式/可量化项目复盘之一。

## 10. 一句话记忆

> **显存手算模板：列状态 → 算 bytes → 标复制/分片 → 加 activation/峰值。**

### 深挖关键点

S 级题必须练到任何参数规模、DP degree 和 ZeRO stage 都能现场推。

## 11. 题源与可信度说明

公开面经中出现或高度近似的高频题型；题干在本项目中做了规范化重写。

> 本项目将“公开面经题型”和“能力等价追问题”明确分开；公开面经用于观察招聘分布，不视为公司官方试卷。

## 12. 延伸阅读

- **R7 · 论文** — [Rajbhandari et al., ZeRO](https://arxiv.org/abs/1910.02054)
- **R9 · 官方文档** — [NVIDIA Megatron Core User Guide](https://docs.nvidia.com/megatron-core/developer-guide/latest/)
- **R15 · 公开面经** — [公开面经：京东大模型算法实习](https://www.nowcoder.com/feed/main/detail/e4ac3528bcdd44ebb8c3617540216932)

## 相关题目

- ← [Q095. 手算一个 Transformer Block 的参数量怎么做？](./095.md)
- → [Q097. 从 0 预训练一个 1B 模型，完整 Pipeline 如何设计？](./097.md)

---

> **复习要求**：能够在不看答案的情况下完成 30 秒结论、3 分钟主答，并承受至少 3 轮 Why/How/Trade-off/Production 追问。
