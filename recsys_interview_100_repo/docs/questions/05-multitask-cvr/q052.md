---
title: "Q052. Shared Bottom 有什么问题？"
chapter: 5
difficulty: "★★★"
source_type: "面经高频"
tags:
  - multitask-cvr
---

# Q052. Shared Bottom 有什么问题？

> **难度**：★★★　 **来源标签**：面经高频　 **章节**：第 5 章 · 多任务、多目标与 CVR 建模

## 题目定位

这道题位于 **第 5 章：多任务、多目标与 CVR 建模**。面试官通常希望候选人能先给定义，再把机制连接到工程后果；最终要落到 **模型/数据/系统/指标** 四个层面的约束，而不是停留在术语复述。

**本章主线**：理解任务关系、负迁移、样本选择偏差、转化漏斗与连续目标建模。

## 30 秒回答

所有任务强制共享同一表示，当任务相关性较弱或目标冲突时，梯度会互相干扰，导致负迁移和“一个任务涨、另一个任务掉”的跷跷板。

## 深入拆解

可以观察 task gradients cosine similarity、各任务 loss 曲线、共享层激活。 简单增加 tower 不能解决共享层冲突。 MMoE/PLE 通过更灵活的共享结构缓解问题。

### 公式 / 工程抓手

`若 ∇L_a · ∇L_b < 0，说明局部梯度方向冲突。`

### PDF 原始公式 / 工程抓手

`若 ∇L_a · ∇L_b < 0，说明局部梯度方向冲突。`

<!-- V2_ENRICHMENT_START -->

## V2 深度版：从“会答”到“能落地”

### 多任务建模的第一性原理

多任务学习的本质是**受控共享**。共享能提高样本效率，但也会通过共享参数传递冲突梯度。分析任何 MTL 模型都可以沿三条线：任务标签空间是否一致、梯度是否冲突、模型如何分配共享/私有容量。

$$L(\theta)=\sum_t \lambda_t L_t(\theta_{shared},\theta_t)$$

真正难点通常不在写出这条式子，而在决定哪些参数该共享、$\lambda_t$ 如何对应业务价值，以及线上多个目标如何合成最终排序。

### 针对本题的关键推导

Shared Bottom 强制所有任务使用同一 representation，无法选择“哪些知识共享、哪些隔离”。当梯度方向冲突时，一个任务的优化会破坏另一个任务。

这里建议把回答进一步拆成四层证据链：

1. **定义层**：一句话说明“Shared Bottom 有什么问题？”讨论的对象和优化目标；
2. **机制层**：给出公式、数据生成过程或计算复杂度，解释为什么会有效；
3. **系统层**：指出它在完整推荐链路中的上游输入、下游依赖、成本和失败传播路径；
4. **验证层**：给出 offline slice、线上 A/B、guardrail 与回滚条件。

### 90 秒标准回答（建议练到可以脱稿）

> Shared Bottom 强制所有任务使用同一 representation，无法选择“哪些知识共享、哪些隔离”。当梯度方向冲突时，一个任务的优化会破坏另一个任务。 具体到工程上，可以用下面的数量级来理解：点击偏好短刺激内容、长期留存偏好高质量内容，两任务梯度可能在共享层方向相反。 因此我不会只看单一离线指标，而会把这项方法放回完整链路，联合检查候选/特征分布、P95/P99、成本以及线上主指标。如果出现“离线好、线上差”，我会优先检查数据与 serving 一致性、截断/近似误差和策略覆盖。最后还要说明边界：任务高度相关、数据规模小或模型简单时 Shared Bottom 仍是很强 baseline，不能一上来就堆 MMoE。

这段 90 秒回答的顺序是 **结论 → 机制 → 数量级 → 工程验证 → 边界**。对于大多数推荐算法题，这比从论文历史开始讲更稳。

### 容易混淆的概念

共享表示 vs 共享专家；任务相关性 vs 标签共现；loss 平衡 vs 业务权重；多任务提升 vs Pareto/跷跷板。

### 上线验证与监控

离线逐任务看 metric、calibration、gradient conflict、gate/expert usage；线上用多指标 Pareto 评价，并设置不能被牺牲的 guardrail。新增任务必须证明共享带来增量而非负迁移。

**建议至少记录这些字段：** task labels、loss scale、task weights、gradient norm/cosine、gate entropy、expert utilization、per-task prediction/calibration。日志字段不是越多越好，关键是能把一次线上曝光追溯到样本、特征、模型、索引和实验版本。

### 一个可用于面试的具体例子

点击偏好短刺激内容、长期留存偏好高质量内容，两任务梯度可能在共享层方向相反。

面试里具体数字的价值不是“显得真实”，而是迫使方案满足数量级约束。即使没有真实业务数据，也可以明确说“下面用数量级举例”，然后说明候选数、样本量、维度、QPS 或延迟如何影响设计。

### 工程决策矩阵

| 决策维度 | 需要监控 |
|---|---|
| Task relation | label overlap、gradient cosine、相关性 |
| Expert/Gate | expert usage、gate entropy、collapse |
| Loss | scale、动态权重、task-specific convergence |
| Calibration | 每任务 reliability / ECE |
| Online | primary + guardrail + Pareto trade-off |

回答时不要把所有维度都平均展开。先指出当前题目的**第一瓶颈**，再解释为什么其他维度是二阶约束，这会比罗列术语更像真实工程决策。

### 参考实现 / 伪代码

```python
shared = shared_or_experts(x)
loss = 0.0
for task in tasks:
    pred = task_head[task](route(task, shared))
    loss += weight[task] * criterion[task](pred, label[task])
loss.backward()
```

这段代码不是为了背 API，而是帮助建立“输入—状态—输出—监控”的工程直觉。真实系统还应记录 feature/model/index/experiment 等版本，确保线上结果能够复现。

### 边界条件与反例

任务高度相关、数据规模小或模型简单时 Shared Bottom 仍是很强 baseline，不能一上来就堆 MMoE。

一个强候选人应该主动说明**什么时候不应该使用当前方法**。这能证明你理解的是归纳偏置与业务条件，而不是模型名称。

### 面试官继续追问时，建议用这套框架

- **追公式**：先定义符号，再说明每一项改变了什么概率/几何/梯度；
- **追为什么**：从数据分布或复杂度出发，不用“实验发现更好”作为唯一理由；
- **追线上**：给 P95/P99、候选数、内存、特征 freshness、回退路径；
- **追效果**：区分离线增量、stage-wise oracle、线上 causal lift；
- **追失败**：给至少一个会让方案失效的分布变化或系统约束。

### Senior / Staff 级加分点

如果面试岗位偏高级，可以进一步讨论：

- **反事实与增量价值**：新模块到底创造了新候选/新信息，还是复制已有能力；
- **Pareto frontier**：质量、延迟、成本、稳定性之间是否存在更优点，而不是只报单指标；
- **可观测性**：出现线上退化时，能否在 10~30 分钟内通过 dashboard/slice 定位到具体 stage；
- **可演进性**：模型升级是否要求全量重建特征/索引，是否支持 shadow、canary 和快速 rollback；
- **组织成本**：跨团队依赖、数据口径与长期维护是否会吞掉理论收益。

### 进一步阅读（原始来源优先）

- **MMoE (KDD 2018)** — https://doi.org/10.1145/3219819.3220007

<!-- V2_ENRICHMENT_END -->

## 工业级工程视角

- Shared Bottom 在弱相关任务上可能产生 gradient conflict；应通过 per-task 分析而非平均 loss 判断。
- 先定义 task graph：哪些 label 是漏斗关系、哪些并列、哪些存在延迟或缺失。
- 监控每个 task 的 loss scale、gradient norm、样本量和 gate/expert utilization。
- 多任务收益必须按 task 分桶检查，防止“平均上涨、关键任务下降”。
- CVR/GMV 等延迟 label 要明确观察窗、归因窗和 late-arriving data 处理。

### 面试中建议主动报出的数字/约束

- 样本量/候选量、离线指标、线上 lift、P95/P99 latency、内存/显存与成本。

## 推荐回答结构（白板版）

1. 先用 1-2 句话定义核心概念或结论。
2. 再解释“为什么”，给出机制、公式或数据生成过程。
3. 补充工业约束：规模、延迟、样本分布、内存/算力或线上一致性。
4. 给出至少一个反例/失败模式，证明你理解适用边界。
5. 最后用线上验证方案收尾：离线分桶 + A/B + guardrail。

## 常见失分

- **PDF 原始失分点**：只回答“任务不同所以不好”，缺少机制。
- 只看平均多任务 loss，不看 task conflict 和分任务收益。
- 只给“最佳实践”结论，不说明适用条件与失败边界。

## 面试评分标准

| 层级 | 面试表现 | 典型特征 |
|---|---|---|
| **Pass** | 能回答定义与主结论 | 复述“所有任务强制共享同一表示，当任务相关性较弱或目标冲突时，梯度会互相干扰，导致负迁移和“一个任务涨、另一个任务…” |
| **Good** | 能讲机制、公式/流程与 1-2 个工程权衡 | 主动讨论数据分布、复杂度、指标或线上约束 |
| **Strong** | 能讨论失败案例、替代方案、监控与 A/B 验证 | 把问题放回完整推荐链路，并能承接 3-5 层追问 |

## 连续追问

1. 哪些任务适合 Shared Bottom？
2. 梯度冲突如何直接处理？
3. 这个结论依赖哪些隐含假设？如果假设不成立会怎样？
4. 你会用哪些离线分桶与线上 guardrail 验证它？
5. 在 QPS/延迟/显存预算减半时，你会如何降级？

## 连续追问参考答案

### 追问 1：哪些任务适合 Shared Bottom？

标签/输入机制相似、梯度方向大体一致、数据规模不足以各训一套的任务。它应作为强 baseline；若已经稳定且无跷跷板，不必为了架构新颖换 MMoE。

### 追问 2：梯度冲突如何直接处理？

可用 PCGrad、CAGrad、GradNorm 等在共享参数梯度层面做投影/平衡，或增加 task-specific capacity。先测 gradient cosine 与任务表现，避免在没有冲突证据时增加复杂度。

### 追问 3：这个结论依赖哪些隐含假设？如果假设不成立会怎样？

假设任务之间存在可共享统计强度。若任务目标本身冲突或 label 机制不同，增加共享会加剧 negative transfer，需要私有容量或重新定义任务。

### 追问 4：你会用哪些离线分桶与线上 guardrail 验证它？

逐任务看 metric/calibration、gradient norm/cosine、gate/expert usage；线上做多目标 A/B，明确 primary 与不可牺牲 guardrail。

### 追问 5：在 QPS/延迟/显存预算减半时，你会如何降级？

减少 expert/hidden size，合并高度相关任务，保留 task heads 与 calibration；避免直接删 guardrail 任务。

## 自测清单

- [ ] 我能在 30-60 秒内先给结论，不绕模型历史。
- [ ] 我能写出关键公式/复杂度，或者画出完整系统链路。
- [ ] 我能说出至少两个失败模式或适用边界。
- [ ] 我能给出 offline → online 的验证方法。
- [ ] 我能承接至少 3 层追问而不回到背诵式答案。

## 相关题目

- [Q051. 为什么推荐系统需要 Multi-Task Learning？](q051.md)
- [Q053. MMoE 怎么解决任务冲突？](q053.md)
- [Q054. MMoE 和普通 MoE 最大区别是什么？](q054.md)
- [Q055. 什么叫多任务“跷跷板现象”？](q055.md)
- [Q056. PLE 相比 MMoE 改进在哪里？](q056.md)
- [Q057. 多任务 Loss 怎么加权？](q057.md)

## 参考资料

- **[I1] 美团推荐算法面经（2025-11）** — https://www.nowcoder.com/feed/main/detail/87082efbdcfb4023b536c06ada8ce1a1
- **[I5] 美团搜推算法：PLE / DIN / In-batch 偏差** — https://www.nowcoder.com/feed/main/detail/a6ca5f8234be43ff964f27535ec35ca0

---

[← 本章目录](index.md) · [知识地图](../../guide/knowledge-map.md) · [项目拷打模板](../../study/project-grilling-template.md)
