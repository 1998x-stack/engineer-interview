---
title: "Q078. IPS 是什么？有什么问题？"
chapter: 7
difficulty: "★★★"
source_type: "面经高频"
tags:
  - bias-data-serving
  - ips
---

# Q078. IPS 是什么？有什么问题？

> **难度**：★★★　 **来源标签**：面经高频　 **章节**：第 7 章 · 偏差、数据、训练与在线工程

## 题目定位

这道题位于 **第 7 章：偏差、数据、训练与在线工程**。面试官通常希望候选人能先给定义，再把机制连接到工程后果；最终要落到 **模型/数据/系统/指标** 四个层面的约束，而不是停留在术语复述。

**本章主线**：关注观测数据的系统性偏差、采样口径、train-serving consistency、漂移与 serving 成本。

## 30 秒回答

IPS 用样本被日志策略选择/曝光的概率倒数做权重，让低曝光概率样本代表更多未观测样本，从而估计目标策略下的无偏期望。主要问题是 propensity 很小时方差爆炸。

## 深入拆解

常用 clipping、SNIPS、自归一化或 Doubly Robust 降方差。 propensity 本身估计错误会引入新偏差。 IPS 适用于 counterfactual evaluation/learning，但需要明确 logging policy。

### 数学 / 机制补充

Inverse Propensity Scoring：

$$\hat L_{IPS}=\frac1N\sum_i \frac{o_i}{p_i}\ell_i$$

当 $p_i$ 很小时权重爆炸、方差很大，因此常配合 clipping、self-normalization 或 doubly robust estimator。

### PDF 原始公式 / 工程抓手

`L_IPS=Σ_i o_i/p_i · L_i`

<!-- V2_ENRICHMENT_START -->

## V2 深度版：从“会答”到“能落地”

### 数据与 Serving 的第一性原理

推荐训练数据来自历史策略选择后的曝光，而不是 iid 抽样。因此数据偏差、训练分布与线上分布、特征时间一致性、模型更新频率是一个整体问题。

工程上应建立**版本化与可观测性**：sample policy、feature schema、model version、index version、serving config 都应可追踪，才能把线上异常定位到具体环节。

### 针对本题的关键推导

IPS 用 1/propensity 重加权恢复目标分布下的无偏估计，但小 propensity 会带来高方差。工程上常 clipping、SNIPS 或 doubly robust。

这里建议把回答进一步拆成四层证据链：

1. **定义层**：一句话说明“IPS 是什么？有什么问题？”讨论的对象和优化目标；
2. **机制层**：给出公式、数据生成过程或计算复杂度，解释为什么会有效；
3. **系统层**：指出它在完整推荐链路中的上游输入、下游依赖、成本和失败传播路径；
4. **验证层**：给出 offline slice、线上 A/B、guardrail 与回滚条件。

### 90 秒标准回答（建议练到可以脱稿）

> IPS 用 1/propensity 重加权恢复目标分布下的无偏估计，但小 propensity 会带来高方差。工程上常 clipping、SNIPS 或 doubly robust。 具体到工程上，可以用下面的数量级来理解：某样本曝光概率 0.01，则权重 100；单个样本就可能主导 batch 梯度，因此常把权重截到 10/20 等上限并分析 bias-variance。 因此我不会只看单一离线指标，而会把这项方法放回完整链路，联合检查候选/特征分布、P95/P99、成本以及线上主指标。如果出现“离线好、线上差”，我会优先检查数据与 serving 一致性、截断/近似误差和策略覆盖。最后还要说明边界：propensity 估计错时 IPS 也会错；必须监控权重分布和 effective sample size。

这段 90 秒回答的顺序是 **结论 → 机制 → 数量级 → 工程验证 → 边界**。对于大多数推荐算法题，这比从论文历史开始讲更稳。

### 容易混淆的概念

相关性修正 vs 因果去偏；class imbalance vs sample selection bias；online inference vs online learning；feature cache vs point-in-time correctness。

### 上线验证与监控

同时监控数据分布、feature freshness、missing/default ratio、model/index version、P99 与 rollback rate。任何去偏或重采样策略要记录 propensity/weight 分布和 effective sample size。

**建议至少记录这些字段：** logging policy、propensity、sample weight、feature event_time、join_time、training snapshot、serving version、drift stats、latency/fallback。日志字段不是越多越好，关键是能把一次线上曝光追溯到样本、特征、模型、索引和实验版本。

### 一个可用于面试的具体例子

某样本曝光概率 0.01，则权重 100；单个样本就可能主导 batch 梯度，因此常把权重截到 10/20 等上限并分析 bias-variance。

面试里具体数字的价值不是“显得真实”，而是迫使方案满足数量级约束。即使没有真实业务数据，也可以明确说“下面用数量级举例”，然后说明候选数、样本量、维度、QPS 或延迟如何影响设计。

### 工程决策矩阵

| 风险 | 应对 |
|---|---|
| Bias | intervention / IPS / model correction |
| Skew | point-in-time feature + shared transform |
| Drift | slice monitor + retrain/reweight trigger |
| Latency | profile critical path + cache/quantize/distill |
| Memory | mixed dimension / quantization / sharding |

回答时不要把所有维度都平均展开。先指出当前题目的**第一瓶颈**，再解释为什么其他维度是二阶约束，这会比罗列术语更像真实工程决策。

### 参考实现 / 伪代码

```text
log policy/version/propensity
        ↓
point-in-time sample join
        ↓
train + slice validation
        ↓
shadow/canary serving
        ↓
monitor drift/skew/latency → retrain or rollback
```

这段代码不是为了背 API，而是帮助建立“输入—状态—输出—监控”的工程直觉。真实系统还应记录 feature/model/index/experiment 等版本，确保线上结果能够复现。

### 边界条件与反例

propensity 估计错时 IPS 也会错；必须监控权重分布和 effective sample size。

一个强候选人应该主动说明**什么时候不应该使用当前方法**。这能证明你理解的是归纳偏置与业务条件，而不是模型名称。

### 面试官继续追问时，建议用这套框架

- **追公式**：先定义符号，再说明每一项改变了什么概率/几何/梯度；
- **追为什么**：从数据分布或复杂度出发，不用“实验发现更好”作为唯一理由；
- **追线上**：给 P95/P99、候选数、内存、特征 freshness、回退路径；
- **追效果**：区分离线增量、stage-wise oracle、线上 causal lift；
- **追失败**：给至少一个会让方案失效的分布变化或系统约束。

### Senior / Staff 级加分点

如果面试岗位偏高级，可以进一步讨论：

- **反事实与增量价值**：新模块到底创造了新候选/新信息，还是复制已有能力；
- **Pareto frontier**：质量、延迟、成本、稳定性之间是否存在更优点，而不是只报单指标；
- **可观测性**：出现线上退化时，能否在 10~30 分钟内通过 dashboard/slice 定位到具体 stage；
- **可演进性**：模型升级是否要求全量重建特征/索引，是否支持 shadow、canary 和快速 rollback；
- **组织成本**：跨团队依赖、数据口径与长期维护是否会吞掉理论收益。

### 进一步阅读（原始来源优先）

- 本题优先结合本页“参考资料”中的原始论文、官方技术材料与公开面经阅读；面经用于理解问法，论文用于校准技术事实。

<!-- V2_ENRICHMENT_END -->

## 工业级工程视角

- IPS 的方差问题往往比 bias 更棘手；clip 上限本身是 bias-variance trade-off。
- 建立 data contract：字段语义、时间戳、缺失策略、版本、生产者/消费者责任。
- 偏差纠正若改变样本权重，要同步检查方差、calibration 与线上稳定性。
- serving 优化按 profiling 结果做，不要默认 GPU/量化一定是瓶颈答案。
- 漂移监控要区分 covariate shift、label shift、concept drift 与业务结构变化。

### 面试中建议主动报出的数字/约束

- 样本量/候选量、离线指标、线上 lift、P95/P99 latency、内存/显存与成本。

## 推荐回答结构（白板版）

1. 先用 1-2 句话定义核心概念或结论。
2. 再解释“为什么”，给出机制、公式或数据生成过程。
3. 补充工业约束：规模、延迟、样本分布、内存/算力或线上一致性。
4. 给出至少一个反例/失败模式，证明你理解适用边界。
5. 最后用线上验证方案收尾：离线分桶 + A/B + guardrail。

## 常见失分

- **PDF 原始失分点**：只背“1/p 加权”，不讲高方差与 positivity assumption。
- 把观测日志当作 i.i.d. 真值，忽略曝光和服务机制。
- 只给“最佳实践”结论，不说明适用条件与失败边界。

## 面试评分标准

| 层级 | 面试表现 | 典型特征 |
|---|---|---|
| **Pass** | 能回答定义与主结论 | 复述“IPS 用样本被日志策略选择/曝光的概率倒数做权重，让低曝光概率样本代表更多未观测样本，从而估计目标策略下的…” |
| **Good** | 能讲机制、公式/流程与 1-2 个工程权衡 | 主动讨论数据分布、复杂度、指标或线上约束 |
| **Strong** | 能讨论失败案例、替代方案、监控与 A/B 验证 | 把问题放回完整推荐链路，并能承接 3-5 层追问 |

## 连续追问

1. 为什么需要 overlap/positivity？
2. DR 为什么更稳？
3. SNIPS 和 IPS 的差别是什么？
4. Doubly Robust 为什么可能更稳？
5. 这个结论依赖哪些隐含假设？如果假设不成立会怎样？

## 连续追问参考答案

### 追问 1：为什么需要 overlap/positivity？

IPS 需要目标动作在日志策略下有非零概率，即 positivity/overlap。若某类 item 从未被旧策略曝光，propensity=0，纯重加权无法估计其反事实效果，只能靠探索或结构假设。

### 追问 2：DR 为什么更稳？

DR 结合 outcome model 与 propensity weighting，只要其中一个模型正确就可获得一致性（在标准假设下），通常比纯 IPS 方差更低。但两个模型都错时仍会偏，且实现/诊断更复杂。

### 追问 3：SNIPS 和 IPS 的差别是什么？

SNIPS 对 IPS 权重做自归一化，用加权和除以权重和，常能降低方差并减小极端权重影响，但会引入有限样本偏差。应同时报告权重分布和 effective sample size。

### 追问 4：Doubly Robust 为什么可能更稳？

DR 结合 outcome model 与 propensity weighting，只要其中一个模型正确就可获得一致性（在标准假设下），通常比纯 IPS 方差更低。但两个模型都错时仍会偏，且实现/诊断更复杂。

### 追问 5：这个结论依赖哪些隐含假设？如果假设不成立会怎样？

假设 bias/propensity/feature time 能被正确记录。若 logging policy 不可恢复或支持集不重叠，纯离线校正无法可靠恢复反事实。

## 自测清单

- [ ] 我能在 30-60 秒内先给结论，不绕模型历史。
- [ ] 我能写出关键公式/复杂度，或者画出完整系统链路。
- [ ] 我能说出至少两个失败模式或适用边界。
- [ ] 我能给出 offline → online 的验证方法。
- [ ] 我能承接至少 3 层追问而不回到背诵式答案。

## 相关题目

- [Q077. Position Bias 怎么解决？](q077.md)
- [Q079. 怎么解决类别不平衡？](q079.md)
- [Q076. 推荐数据有哪些常见 Bias？](q076.md)
- [Q080. 为什么训练样本不能随便随机负采样？](q080.md)
- [Q081. 什么是 Train-Serving Skew？](q081.md)
- [Q082. 在线训练和离线训练有什么区别？](q082.md)

## 参考资料

- **[I2] 26 校招美团推荐算法一面（2025-12）** — https://www.nowcoder.com/feed/main/detail/16e5b9c65159469983165ce7f37c1884
- **[I4] 快手推荐算法实习一面面经** — https://www.nowcoder.com/feed/main/detail/386ed2e833f44d299cdaf180d0257c06

---

[← 本章目录](index.md) · [知识地图](../../guide/knowledge-map.md) · [项目拷打模板](../../study/project-grilling-template.md)
