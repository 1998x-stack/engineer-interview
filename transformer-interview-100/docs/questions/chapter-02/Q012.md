---
id: Q012
title: "Q012 为什么必须除以 √d_k？"
chapter: 2
difficulty: "3/5"
tags: [transformer]
---

# Q012 · 为什么必须除以 √d_k？

> **章节**：2. Attention 数学与实现细节  
> **难度**：3/5 · **频率**：高频 · **形式**：概念 / 推导 / 追问  
> **内容分层**：`PDF Core` 来自原 PDF 的观点重排；`Professional Expansion` 为在不改变 PDF 主结论基础上的原创扩展。

## 1. 面试官在考什么 — PDF Core

要求真正推导 dot-product 方差与 Softmax 饱和，而不是背一句“稳定梯度”。

## 2. 30 秒回答 — PDF Core

> 若 q_i、k_i 独立、均值 0、方差 1，则 q·k=Σq_ik_i 的方差约为 d_k，标准差约为 √d_k。维度增大时 logit 绝对值会 变大，Softmax 更尖锐、梯度更容易进入饱和区。除以 √d_k 把典型尺度拉回 O(1)。 Var(q·k)≈d_k ⇒ std≈√d_k

## 3. 核心公式 / Shape

若每维独立且方差 1，则 $\mathrm{Var}(q\cdot k)\approx d_k$，标准差约 $\sqrt{d_k}$。

## 4. 深度拆解 — PDF Core

- 关键是点积由 d_k 个随机项求和。
- 如果 q/k 的初始化方差不是 1，实际 logit 方差也会相应变化，但 √d_k 是结构上的主缩放。
- 缩放不是为了“把概率除小”，而是发生在 Softmax 前的 logits。

## 5. Professional Expansion：为什么这题值得深挖

若 q_i,k_i 独立零均值单位方差，点积方差随 d_k 线性增长；除 √d_k 使 logit 标准差保持 O(1)，避免 Softmax 过早饱和。

### 5.1 工程实现 / 系统视角

记录不同 d_head 下未缩放与缩放 logits 的 std、entropy 和 gradient norm。

### 5.2 边界条件与反例

这是假设近似推导，不意味着真实激活严格独立同分布。

### 5.3 建议亲手做的验证实验

- d_head=16/64/256 做 entropy 曲线。
- 记录至少一种 **correctness metric** 与一种 **performance metric**；不要只凭“代码能运行”判定实现正确。

### 5.4 3 分钟专业版答法

面试时可以按下面节奏展开：

> **第一句给结论**：若 q_i、k_i 独立、均值 0、方差 1，则 q·k=Σq_ik_i 的方差约为 d_k，标准差约为 √d_k。维度增大时 logit 绝对值会 变大，Softmax 更尖锐、梯度更容易进入饱和区。除以 √d_k 把典型尺度拉回 O(1)。 Var(q·k)≈d_k ⇒ std≈√d_k  
> **第二层讲机制**：关键是点积由 d_k 个随机项求和。；如果 q/k 的初始化方差不是 1，实际 logit 方差也会相应变化，但 √d_k 是结构上的主缩放。；缩放不是为了“把概率除小”，而是发生在 Softmax 前的 logits。  
> **第三层补工程条件**：把本题放到真实 Tensor Shape、训练/推理阶段与硬件约束下重新检查；如果结论涉及“更快/更省/更稳定”，必须说明比较基线和适用条件。  
> **最后主动给验证**：用最小行为测试、数值 parity、profiler 或 ablation 证明，而不是停留在直觉。

### 5.5 复杂度与资源矩阵

| 维度 | 本题应如何分析 |
|---|---|
| 参数量 | 标准 MHA 投影约 $4d^2$；若涉及 GQA/MQA 则单独计算 K/V 投影。 |
| FLOPs | $QK^T$ 与 $PV$ 约 $O(T_qT_kd)$；投影仍有 $O(Td^2)$。 |
| 显存 / IO | 朴素 attention 可能 materialize score/probability；高性能实现重点减少 HBM traffic。 |
| 数值稳定 | scale、mask、Softmax max-subtraction、fully-masked row 是高频风险。 |
| 主要验证 | 概率和、causal leakage、naive-vs-fused parity。 |

### 5.6 本题关键不变量

- Softmax 通常沿 key 维归一化。
- Mask 必须与 score 可广播，非法 key 概率应为 0。
- 输出 shape 必须回到 residual stream 所需的 hidden width。

## 6. 面试中的推导顺序

1. **先定义**：一句话说明对象是什么、输入输出是什么。
2. **再公式**：写最小核心公式，并标出关键维度。
3. **解释 Why**：它解决前一代方案的什么瓶颈。
4. **算成本**：参数、FLOPs、activation/KV memory、IO/通信分别看。
5. **讲 Gotcha**：至少指出一个数值或实现陷阱。
6. **收束 Trade-off**：明确什么条件下它不一定更优。

## 7. 常见失分点 — PDF Core

- 只说“防止梯度爆炸”，没有说明 Softmax 饱和。

## 8. 面试官继续追问 — PDF Core

> 如果改成除以 d_k，会发生什么？温度会如何变化？

### 扩展追问树

- 如果把关键超参数扩大 4 倍，**参数量 / FLOPs / 显存 / latency** 哪一项最先成为瓶颈？
- 如果在 **训练** 与 **增量推理** 两个阶段分别使用，本题结论有哪些不同？
- 如何写一个最小单元测试或 ablation，证明你的解释而不是只靠直觉？

## 9. 一语中的 — PDF Core

**若 q_i、k_i 独立、均值 0、方差 1，则 q·k=Σq_ik_i 的方差约为 d_k，标准差约为 √d_k。**

## 10. 面试评分 Rubric

| 档位 | 面试表现 |
|---|---|
| 及格 | 能给出 PDF 的 30 秒主干，不出现概念性错误。 |
| 良好 | 能写核心公式/shape，并解释为什么这样设计。 |
| 优秀 | 能讨论复杂度、数值/工程 gotcha，并回答至少 2 层追问。 |
| 强 Hire 信号 | 能把问题连接到训练/推理/硬件约束，主动提出验证实验或 ablation。 |

> 本题标注难度：**3/5**。难度越高，越看重 trade-off 和验证方法，而不只是定义。

## 11. 相关题目

- [Q014 Softmax 如何避免数值溢出？](../chapter-02/Q014.md)
- [Q080 FlashAttention 到底优化了什么？](../chapter-07/Q080.md)
- [Q082 Online Softmax 如何分块仍保持精确？](../chapter-08/Q082.md)

## 12. 延伸阅读

- [Attention Is All You Need](https://arxiv.org/abs/1706.03762)

<!-- V2-EXPANSION-START -->

## 13. 深入推导：把结论变成可验证的模型

若 $q_i,k_i$ 近似独立、零均值、单位方差，则 $q\cdot k$ 的方差约为 $d_k$，标准差约 $\sqrt{d_k}$。除以 $\sqrt{d_k}$ 将 logits 的典型尺度控制在近似常数区间，避免 softmax 初期过度尖锐。

更严格地说，这个推导依赖初始化与分布假设；训练后 Q/K 不再独立同分布。因此 scale 的价值是提供良好的初始与稳定数值尺度，而不是保证任意时刻 logits 方差恰好为 1。

### 13.1 本章心智模型

> 把 Attention 当作可推导的数值算法：score、scale、mask、softmax、复杂度和解释边界。

这意味着面试时不要停在术语层。每个结论至少回答四个问题：**成立所需假设是什么？张量/概率对象是什么？成本落在哪里？怎样设计反例或单测证伪？**

## 14. 题目特异的工程 Checklist

- 不只写 big-O：把计算、HBM IO、网络通信、allocator 与调度分别量化。
- 任何“更快”都说明硬件、batch/context 分布、精度和质量基线。
- 同时看平均与 P95/P99，防止吞吐优化牺牲尾延迟。
- 设计 rollback/quality regression，确保系统优化没有改变模型语义。

## 15. 推荐验证协议

1. **Parameterized unit tests**：覆盖不同 B/T/H/dtype/mask。
2. **Behavioural test**：测试未来泄漏、padding invariance、cache reset 等语义。
3. **Oracle comparison**：优化代码与朴素 reference 比较输出和梯度。

验证结果建议至少记录：

| 类型 | 最低要求 |
|---|---|
| Correctness | reference parity / invariant / leakage test 至少一项 |
| Numerical | NaN/Inf、max/RMS、dtype 差异 |
| Performance | wall-clock、显存/bytes、P95/P99（系统题） |
| Quality | loss/perplexity/任务指标；结构改动必须做回归 |

## 16. 面试官真正想听到的“边界条件”

- **不要把渐进复杂度等同于实际速度**：kernel、batch、长度、硬件与 IO 会改变主导项。
- **不要把一种实现当成数学定义**：mask 语义、layout、fused kernel、cache allocator 都可能变化。
- **不要把单点 benchmark 当成普遍规律**：至少说明 $B,T,d,H$、dtype、device 和任务分布。
- **不要把“能运行”当成“正确”**：Transformer 很多 bug 会正常降 loss，但存在未来泄漏、错误广播或 cache position 错位。

## 17. Whiteboard / Coding 表达模板

当面试官要求现场推导或编码，可按以下顺序：

1. 写输入输出 shape 与符号定义；
2. 写最小核心公式，不先写优化；
3. 写 2–3 个 invariants/assertions；
4. 给 reference 实现或手算小例子；
5. 再讨论 fused kernel、cache、并行或量化；
6. 最后给测试与 benchmark 设计。

> **Strong Hire 信号**：不仅能解释“为什么”，还能说出“什么情况下不成立，以及我会怎么证明”。

<!-- V2-EXPANSION-END -->

---

← [Q011](../chapter-02/Q011.md) · [返回 100 题索引](../../guide/question-index.md) · [Q013](../chapter-02/Q013.md) →
