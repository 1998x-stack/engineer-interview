---
id: Q046
title: "Q046 BERT 为什么可以双向？"
chapter: 5
difficulty: "2/5"
tags: [bert-gpt]
---

# Q046 · BERT 为什么可以双向？

> **章节**：5. BERT、GPT 与 Encoder-Decoder  
> **难度**：2/5 · **频率**：高频 · **形式**：概念 / 推导 / 追问  
> **内容分层**：`PDF Core` 来自原 PDF 的观点重排；`Professional Expansion` 为在不改变 PDF 主结论基础上的原创扩展。

## 1. 面试官在考什么 — PDF Core

理解双向上下文与 masking objective。

## 2. 30 秒回答 — PDF Core

> BERT Encoder 没有 causal mask，位置 i 可以同时读取左、右上下文，因此每层表示都联合条件于两侧 token。为避 免直接看到被预测 token，本质训练目标采用 MLM。

## 3. 核心公式 / Shape

BERT token $i$ 可 attend 到 $j<i$ 与 $j>i$（padding 除外）。

## 4. 深度拆解 — PDF Core

- 双向指的是 attention visibility，不是两个 RNN 方向拼接。
- 预训练时被 mask 的 token 需要用上下文恢复。
- 下游 fine-tuning 时通常不再随机 mask 输入。

## 5. Professional Expansion：为什么这题值得深挖

BERT 无 causal mask，位置 i 可读取左右文，因此得到双向上下文化 representation；这与自回归概率分解不同。

### 5.1 工程实现 / 系统视角

padding mask 仍需存在。

### 5.2 边界条件与反例

双向不意味着使用未来标签，MLM 通过遮挡目标避免直接泄露。

### 5.3 建议亲手做的验证实验

- 画 BERT 与 GPT attention mask。
- 记录至少一种 **correctness metric** 与一种 **performance metric**；不要只凭“代码能运行”判定实现正确。

### 5.4 3 分钟专业版答法

面试时可以按下面节奏展开：

> **第一句给结论**：BERT Encoder 没有 causal mask，位置 i 可以同时读取左、右上下文，因此每层表示都联合条件于两侧 token。为避 免直接看到被预测 token，本质训练目标采用 MLM。  
> **第二层讲机制**：双向指的是 attention visibility，不是两个 RNN 方向拼接。；预训练时被 mask 的 token 需要用上下文恢复。；下游 fine-tuning 时通常不再随机 mask 输入。  
> **第三层补工程条件**：把本题放到真实 Tensor Shape、训练/推理阶段与硬件约束下重新检查；如果结论涉及“更快/更省/更稳定”，必须说明比较基线和适用条件。  
> **最后主动给验证**：用最小行为测试、数值 parity、profiler 或 ablation 证明，而不是停留在直觉。

### 5.5 复杂度与资源矩阵

| 维度 | 本题应如何分析 |
|---|---|
| 监督密度 | MLM 只在 mask 位计主要监督；CLM 通常几乎每个有效 token 都贡献 next-token loss。 |
| Mask | 双向/causal 可见性决定信息流，不要只从“模型名字”判断。 |
| 训练-推理差异 | Teacher Forcing 与 autoregressive rollout 的条件分布不同。 |
| 任务匹配 | 分类/检索/生成/seq2seq 的接口和延迟要求会影响架构选择。 |
| 主要验证 | label alignment、mask 可见性、下游任务与生成行为。 |

### 5.6 本题关键不变量

- 当前预测位置不能直接看到其监督目标。
- padding/无效 token 不应污染 attention 或 loss。
- 训练目标与推理方式的差异必须显式说明。

## 6. 面试中的推导顺序

1. **先定义**：一句话说明对象是什么、输入输出是什么。
2. **再公式**：写最小核心公式，并标出关键维度。
3. **解释 Why**：它解决前一代方案的什么瓶颈。
4. **算成本**：参数、FLOPs、activation/KV memory、IO/通信分别看。
5. **讲 Gotcha**：至少指出一个数值或实现陷阱。
6. **收束 Trade-off**：明确什么条件下它不一定更优。

## 7. 常见失分点 — PDF Core

- 说“因为 BERT 同时从左到右和从右到左训练两个模型”。

## 8. 面试官继续追问 — PDF Core

> 为什么直接 next-token prediction 无法在同一位置使用未来 token？

### 扩展追问树

- 如果把关键超参数扩大 4 倍，**参数量 / FLOPs / 显存 / latency** 哪一项最先成为瓶颈？
- 如果在 **训练** 与 **增量推理** 两个阶段分别使用，本题结论有哪些不同？
- 如何写一个最小单元测试或 ablation，证明你的解释而不是只靠直觉？

## 9. 一语中的 — PDF Core

**BERT Encoder 没有 causal mask，位置 i 可以同时读取左、右上下文，因此每层表示都联合条件于两侧 token。**

## 10. 面试评分 Rubric

| 档位 | 面试表现 |
|---|---|
| 及格 | 能给出 PDF 的 30 秒主干，不出现概念性错误。 |
| 良好 | 能写核心公式/shape，并解释为什么这样设计。 |
| 优秀 | 能讨论复杂度、数值/工程 gotcha，并回答至少 2 层追问。 |
| 强 Hire 信号 | 能把问题连接到训练/推理/硬件约束，主动提出验证实验或 ablation。 |

> 本题标注难度：**2/5**。难度越高，越看重 trade-off 和验证方法，而不只是定义。

## 11. 相关题目

- [Q045 Encoder-only、Decoder-only、Encoder-Decoder 的本质区别？](../chapter-05/Q045.md)
- [Q047 BERT 为什么用 MLM，而 GPT 用 CLM？](../chapter-05/Q047.md)

## 12. 延伸阅读

- [Attention Is All You Need](https://arxiv.org/abs/1706.03762)
- [BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding](https://arxiv.org/abs/1810.04805)
- [RoBERTa](https://arxiv.org/abs/1907.11692)

<!-- V2-EXPANSION-START -->

## 13. 深入推导：把结论变成可验证的模型

BERT “双向”是指每个非 mask token 的 self-attention 能访问左右两侧上下文，而不是两个独立方向的 RNN。由于目标 token 被遮挡，模型不能直接复制答案。

注意 `[MASK]` 只在预训练出现会造成 pretrain-finetune discrepancy，后来有 replaced token detection、span corruption 等替代目标。

### 13.1 本章心智模型

> 从 attention 可见性与概率分解理解 BERT/GPT/encoder-decoder，而不是只背模型名。

这意味着面试时不要停在术语层。每个结论至少回答四个问题：**成立所需假设是什么？张量/概率对象是什么？成本落在哪里？怎样设计反例或单测证伪？**

## 14. 题目特异的工程 Checklist

**bert-gpt 检查表**

- 先写 attention 可见性与训练 objective，再讨论任务适配。
- 区分预训练目标的统计性质与下游 fine-tuning/serving 形态。
- 评估时控制 tokenizer、数据量与参数规模，避免不公平比较。
- 对生成模型同时讨论 teacher-forced training 与 free-running inference。

## 15. 推荐验证协议

1. **Objective sanity**：构造可人工计算的 token/label，验证 mask 与 shift。
2. **Leakage test**：改变不可见 token，当前位置输出不应变化。
3. **Task ablation**：在固定参数/数据预算下比较 objective 或架构变体。

验证结果建议至少记录：

| 类型 | 最低要求 |
|---|---|
| Correctness | reference parity / invariant / leakage test 至少一项 |
| Numerical | NaN/Inf、max/RMS、dtype 差异 |
| Performance | wall-clock、显存/bytes、P95/P99（系统题） |
| Quality | loss/perplexity/任务指标；结构改动必须做回归 |

## 16. 面试官真正想听到的“边界条件”

- **不要把渐进复杂度等同于实际速度**：kernel、batch、长度、硬件与 IO 会改变主导项。
- **不要把一种实现当成数学定义**：mask 语义、layout、fused kernel、cache allocator 都可能变化。
- **不要把单点 benchmark 当成普遍规律**：至少说明 $B,T,d,H$、dtype、device 和任务分布。
- **不要把“能运行”当成“正确”**：Transformer 很多 bug 会正常降 loss，但存在未来泄漏、错误广播或 cache position 错位。

## 17. Whiteboard / Coding 表达模板

当面试官要求现场推导或编码，可按以下顺序：

1. 写输入输出 shape 与符号定义；
2. 写最小核心公式，不先写优化；
3. 写 2–3 个 invariants/assertions；
4. 给 reference 实现或手算小例子；
5. 再讨论 fused kernel、cache、并行或量化；
6. 最后给测试与 benchmark 设计。

> **Strong Hire 信号**：不仅能解释“为什么”，还能说出“什么情况下不成立，以及我会怎么证明”。

<!-- V2-EXPANSION-END -->

---

← [Q045](../chapter-05/Q045.md) · [返回 100 题索引](../../guide/question-index.md) · [Q047](../chapter-05/Q047.md) →
