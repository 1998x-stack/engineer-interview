---
id: Q063
title: "Q063 训练正常但验证每次结果不同，先查什么？"
chapter: 6
difficulty: "2/5"
tags: [transformer]
---

# Q063 · 训练正常但验证每次结果不同，先查什么？

> **章节**：6. 训练、优化与分布式  
> **难度**：2/5 · **频率**：高频 · **形式**：概念 / 推导 / 追问  
> **内容分层**：`PDF Core` 来自原 PDF 的观点重排；`Professional Expansion` 为在不改变 PDF 主结论基础上的原创扩展。

## 1. 面试官在考什么 — PDF Core

考察最基本的 train/eval 状态与随机性排查。

## 2. 30 秒回答 — PDF Core

> 先确认 model.eval()，使 dropout 等随机层进入推理模式；再检查随机采样、seed、data order、非确定性 kernel 与分布式 sampler。

## 3. 核心公式 / Shape

推理随机性可能来自 dropout、sampling、数据顺序与非确定性 kernel。

## 4. 深度拆解 — PDF Core

- 如果使用生成评测，temperature/top-p 也会引入随机性。
- eval() 不会自动关闭梯度，通常还配合 no_grad/inference_mode。
- 某些模型使用 stochastic depth 或 router noise，也需确认评估配置。

## 5. Professional Expansion：为什么这题值得深挖

先检查 eval 模式、seed、sampling 和数据顺序，再检查非确定性 kernel/分布式归约。

### 5.1 工程实现 / 系统视角

validation generation 若用 temperature>0 本来就会随机。

### 5.2 边界条件与反例

固定 seed 不保证所有 GPU kernel bitwise deterministic。

### 5.3 建议亲手做的验证实验

- 写 reproducibility checklist 自动记录环境。
- 记录至少一种 **correctness metric** 与一种 **performance metric**；不要只凭“代码能运行”判定实现正确。

### 5.4 3 分钟专业版答法

面试时可以按下面节奏展开：

> **第一句给结论**：先确认 model.eval()，使 dropout 等随机层进入推理模式；再检查随机采样、seed、data order、非确定性 kernel 与分布式 sampler。  
> **第二层讲机制**：如果使用生成评测，temperature/top-p 也会引入随机性。；eval() 不会自动关闭梯度，通常还配合 no_grad/inference_mode。；某些模型使用 stochastic depth 或 router noise，也需确认评估配置。  
> **第三层补工程条件**：把本题放到真实 Tensor Shape、训练/推理阶段与硬件约束下重新检查；如果结论涉及“更快/更省/更稳定”，必须说明比较基线和适用条件。  
> **最后主动给验证**：用最小行为测试、数值 parity、profiler 或 ablation 证明，而不是停留在直觉。

### 5.5 复杂度与资源矩阵

| 维度 | 本题应如何分析 |
|---|---|
| 显存 | 权重、梯度、optimizer state、activation 四类必须分开估算。 |
| 数值 | FP16/BF16、loss scaling、grad clipping、NaN 定位都要看 first non-finite。 |
| 步数语义 | gradient accumulation 后 scheduler/clip/optimizer 应按 optimizer-step 定义。 |
| 通信 | DP/TP/PP/CP 的收益受网络拓扑与 collective 开销约束。 |
| 主要验证 | 固定 batch 参数 delta、layerwise grad、throughput/MFU、恢复一致性。 |

### 5.6 本题关键不变量

- 任何 NaN 都先找 first non-finite，而不是先调参。
- optimizer/scheduler/clip 的 step 语义必须与 accumulation 一致。
- 多 GPU 方案要同时满足显存约束和通信可承受。

## 6. 面试中的推导顺序

1. **先定义**：一句话说明对象是什么、输入输出是什么。
2. **再公式**：写最小核心公式，并标出关键维度。
3. **解释 Why**：它解决前一代方案的什么瓶颈。
4. **算成本**：参数、FLOPs、activation/KV memory、IO/通信分别看。
5. **讲 Gotcha**：至少指出一个数值或实现陷阱。
6. **收束 Trade-off**：明确什么条件下它不一定更优。

## 7. 常见失分点 — PDF Core

- 只设置 torch.manual_seed 就以为完全确定。

## 8. 面试官继续追问 — PDF Core

> 为何 CUDA 某些 kernel 即使固定 seed 仍可能非确定？

### 扩展追问树

- 如果把关键超参数扩大 4 倍，**参数量 / FLOPs / 显存 / latency** 哪一项最先成为瓶颈？
- 如果在 **训练** 与 **增量推理** 两个阶段分别使用，本题结论有哪些不同？
- 如何写一个最小单元测试或 ablation，证明你的解释而不是只靠直觉？

## 9. 一语中的 — PDF Core

**先确认 model.eval()，使 dropout 等随机层进入推理模式；再检查随机采样、seed、data order、非确定性 kernel 与分布 式 sampler。**

## 10. 面试评分 Rubric

| 档位 | 面试表现 |
|---|---|
| 及格 | 能给出 PDF 的 30 秒主干，不出现概念性错误。 |
| 良好 | 能写核心公式/shape，并解释为什么这样设计。 |
| 优秀 | 能讨论复杂度、数值/工程 gotcha，并回答至少 2 层追问。 |
| 强 Hire 信号 | 能把问题连接到训练/推理/硬件约束，主动提出验证实验或 ablation。 |

> 本题标注难度：**2/5**。难度越高，越看重 trade-off 和验证方法，而不只是定义。

## 11. 相关题目

- [Q062 FP16 与 BF16 为什么训练表现不同？](../chapter-06/Q062.md)
- [Q064 Transformer 初始化不合理有哪些症状？](../chapter-06/Q064.md)

## 12. 延伸阅读

- [Attention Is All You Need](https://arxiv.org/abs/1706.03762)

<!-- V2-EXPANSION-START -->

## 13. 深入推导：把结论变成可验证的模型

验证结果随机首先检查 `model.eval()`、dropout、generation sampling seed；其次检查数据顺序、分布式 sampler、非确定 CUDA kernel 和并行 reduction。要把“统计随机性”和“实现 bug”分开。

最小复现策略：固定单样本、单卡、greedy decode、确定性设置，重复 forward；若仍不同，再逐层比较中间 activation。

### 13.1 本章心智模型

> 建立训练故障树：objective、loss mask、数值精度、optimizer、并行和 scaling。

这意味着面试时不要停在术语层。每个结论至少回答四个问题：**成立所需假设是什么？张量/概率对象是什么？成本落在哪里？怎样设计反例或单测证伪？**

## 14. 题目特异的工程 Checklist

- 不只写 big-O：把计算、HBM IO、网络通信、allocator 与调度分别量化。
- 任何“更快”都说明硬件、batch/context 分布、精度和质量基线。
- 同时看平均与 P95/P99，防止吞吐优化牺牲尾延迟。
- 设计 rollback/quality regression，确保系统优化没有改变模型语义。

## 15. 推荐验证协议

1. **Parameterized unit tests**：覆盖不同 B/T/H/dtype/mask。
2. **Behavioural test**：测试未来泄漏、padding invariance、cache reset 等语义。
3. **Oracle comparison**：优化代码与朴素 reference 比较输出和梯度。

验证结果建议至少记录：

| 类型 | 最低要求 |
|---|---|
| Correctness | reference parity / invariant / leakage test 至少一项 |
| Numerical | NaN/Inf、max/RMS、dtype 差异 |
| Performance | wall-clock、显存/bytes、P95/P99（系统题） |
| Quality | loss/perplexity/任务指标；结构改动必须做回归 |

## 16. 面试官真正想听到的“边界条件”

- **不要把渐进复杂度等同于实际速度**：kernel、batch、长度、硬件与 IO 会改变主导项。
- **不要把一种实现当成数学定义**：mask 语义、layout、fused kernel、cache allocator 都可能变化。
- **不要把单点 benchmark 当成普遍规律**：至少说明 $B,T,d,H$、dtype、device 和任务分布。
- **不要把“能运行”当成“正确”**：Transformer 很多 bug 会正常降 loss，但存在未来泄漏、错误广播或 cache position 错位。

## 17. Whiteboard / Coding 表达模板

当面试官要求现场推导或编码，可按以下顺序：

1. 写输入输出 shape 与符号定义；
2. 写最小核心公式，不先写优化；
3. 写 2–3 个 invariants/assertions；
4. 给 reference 实现或手算小例子；
5. 再讨论 fused kernel、cache、并行或量化；
6. 最后给测试与 benchmark 设计。

> **Strong Hire 信号**：不仅能解释“为什么”，还能说出“什么情况下不成立，以及我会怎么证明”。

<!-- V2-EXPANSION-END -->

---

← [Q062](../chapter-06/Q062.md) · [返回 100 题索引](../../guide/question-index.md) · [Q064](../chapter-06/Q064.md) →
