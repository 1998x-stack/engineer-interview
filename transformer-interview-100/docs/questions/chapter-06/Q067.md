---
id: Q067
title: "Q067 Decoder-only LLM 的生成过程是什么？"
chapter: 6
difficulty: "2/5"
tags: [inference]
---

# Q067 · Decoder-only LLM 的生成过程是什么？

> **章节**：6. 训练、优化与分布式  
> **难度**：2/5 · **频率**：高频 · **形式**：概念 / 推导 / 追问  
> **内容分层**：`PDF Core` 来自原 PDF 的观点重排；`Professional Expansion` 为在不改变 PDF 主结论基础上的原创扩展。

## 1. 面试官在考什么 — PDF Core

确保真正理解 autoregressive dependency。

## 2. 30 秒回答 — PDF Core

> 给定 prefix x₁:T，模型预测 P(x_{T+1}|x_≤T)，采样/选择新 token 后把它追加，再预测下一步。输出 N 个 token 至 少存在 N 个依赖的 decoding steps。

## 3. 核心公式 / Shape

$x_{T+1}\sim P(\cdot|x_{\le T})$，追加后重复。

## 4. 深度拆解 — PDF Core

- prefill 可并行处理整个 prompt。
- decode 每步通常只新增 1 token。
- sampling policy 决定从 logits 到 token 的选择方式。

## 5. Professional Expansion：为什么这题值得深挖

生成是循环：prefill prefix→得到 next-token distribution→选 token→增量 forward→重复；性能由 TTFT 与逐 token decode 共同决定。

### 5.1 工程实现 / 系统视角

区分 stopping、sampling、cache 更新和 streaming。

### 5.2 边界条件与反例

不要把生成等同于每次重算全部 prefix。

### 5.3 建议亲手做的验证实验

- 写最小 greedy generation loop。
- 记录至少一种 **correctness metric** 与一种 **performance metric**；不要只凭“代码能运行”判定实现正确。

### 5.4 3 分钟专业版答法

面试时可以按下面节奏展开：

> **第一句给结论**：给定 prefix x₁:T，模型预测 P(x_{T+1}|x_≤T)，采样/选择新 token 后把它追加，再预测下一步。输出 N 个 token 至 少存在 N 个依赖的 decoding steps。  
> **第二层讲机制**：prefill 可并行处理整个 prompt。；decode 每步通常只新增 1 token。；sampling policy 决定从 logits 到 token 的选择方式。  
> **第三层补工程条件**：把本题放到真实 Tensor Shape、训练/推理阶段与硬件约束下重新检查；如果结论涉及“更快/更省/更稳定”，必须说明比较基线和适用条件。  
> **最后主动给验证**：用最小行为测试、数值 parity、profiler 或 ablation 证明，而不是停留在直觉。

### 5.5 复杂度与资源矩阵

| 维度 | 本题应如何分析 |
|---|---|
| 显存 | 权重、梯度、optimizer state、activation 四类必须分开估算。 |
| 数值 | FP16/BF16、loss scaling、grad clipping、NaN 定位都要看 first non-finite。 |
| 步数语义 | gradient accumulation 后 scheduler/clip/optimizer 应按 optimizer-step 定义。 |
| 通信 | DP/TP/PP/CP 的收益受网络拓扑与 collective 开销约束。 |
| 主要验证 | 固定 batch 参数 delta、layerwise grad、throughput/MFU、恢复一致性。 |

### 5.6 本题关键不变量

- 任何 NaN 都先找 first non-finite，而不是先调参。
- optimizer/scheduler/clip 的 step 语义必须与 accumulation 一致。
- 多 GPU 方案要同时满足显存约束和通信可承受。

## 6. 面试中的推导顺序

1. **先定义**：一句话说明对象是什么、输入输出是什么。
2. **再公式**：写最小核心公式，并标出关键维度。
3. **解释 Why**：它解决前一代方案的什么瓶颈。
4. **算成本**：参数、FLOPs、activation/KV memory、IO/通信分别看。
5. **讲 Gotcha**：至少指出一个数值或实现陷阱。
6. **收束 Trade-off**：明确什么条件下它不一定更优。

## 7. 常见失分点 — PDF Core

- 把模型一次 forward 输出的所有位置 logits 当成可以一次生成未来 N 个 token。

## 8. 面试官继续追问 — PDF Core

> 为什么 speculative decoding 可以“一次推进多个 token”却仍需 target model 验证？

### 扩展追问树

- 如果把关键超参数扩大 4 倍，**参数量 / FLOPs / 显存 / latency** 哪一项最先成为瓶颈？
- 如果在 **训练** 与 **增量推理** 两个阶段分别使用，本题结论有哪些不同？
- 如何写一个最小单元测试或 ablation，证明你的解释而不是只靠直觉？

## 9. 一语中的 — PDF Core

**给定 prefix x₁:T，模型预测 P(x_{T+1}|x_≤T)，采样/选择新 token 后把它追加，再预测下一步。 第七章 自回归推理与 KV Cache 从增量解码推导 KV Cache、GQA/MQA、采样、TTFT/TPOT 与 serving 内存。 题号 题目 难度 Q068 KV Cache 缓存了什么，为什么不缓存 Q？ 3/5 Q069 KV Cache 内存如何估算？ 4/5 Q070 为什么训练不需要像 decode 一样使用 KV 3/5 Cache？ Q071 Prefill 与 Decode 的瓶颈有什么不同？ 4/5 Q072 MHA、MQA、GQA 的区别？ 3/5 Q073 Greedy、Beam Search 与 Sampling 如何 3/5 选择？ Q074 Temperature、Top-k、Top-p 分别做什 2/5 么？ Q075 为什么模型会陷入重复生成？ 3/5 Q076 LLM Serving 的关键 latency 指标有哪些？ 3/5 Q077 Continuous Batching 为什么适合 LLM？ 4/5 Q078 PagedAttention 解决什么问题？ 4/5 Q079 Speculative Decoding 为什么能加速且保持 4/5 目标分布？ Q080 FlashAttention 到底优化了什么？ 3/5**

## 10. 面试评分 Rubric

| 档位 | 面试表现 |
|---|---|
| 及格 | 能给出 PDF 的 30 秒主干，不出现概念性错误。 |
| 良好 | 能写核心公式/shape，并解释为什么这样设计。 |
| 优秀 | 能讨论复杂度、数值/工程 gotcha，并回答至少 2 层追问。 |
| 强 Hire 信号 | 能把问题连接到训练/推理/硬件约束，主动提出验证实验或 ablation。 |

> 本题标注难度：**2/5**。难度越高，越看重 trade-off 和验证方法，而不只是定义。

## 11. 相关题目

- [Q066 Transformer 多 GPU 有哪些并行方式？](../chapter-06/Q066.md)
- [Q068 KV Cache 缓存了什么，为什么不缓存 Q？](../chapter-07/Q068.md)

## 12. 延伸阅读

- [Attention Is All You Need](https://arxiv.org/abs/1706.03762)

<!-- V2-EXPANSION-START -->

## 13. 深入推导：把结论变成可验证的模型

Decoder-only 生成每步根据当前 prefix 得到 next-token distribution，应用 decoding policy 选 token，再追加到 prefix。使用 KV cache 后每层只计算新 token 的 Q/K/V，并复用历史 K/V。

生成停止条件包括 EOS、max tokens、stop sequence 等；在 batch serving 中不同请求结束时间不同，这直接导致 continuous batching 与动态 KV allocator 的必要性。

### 13.1 本章心智模型

> 建立训练故障树：objective、loss mask、数值精度、optimizer、并行和 scaling。

这意味着面试时不要停在术语层。每个结论至少回答四个问题：**成立所需假设是什么？张量/概率对象是什么？成本落在哪里？怎样设计反例或单测证伪？**

## 14. 题目特异的工程 Checklist

**inference 检查表**

- 分开 prefill 与 decode：前者偏大 GEMM，后者偏 KV bandwidth/调度。
- 指标至少报告 TTFT、TPOT/ITL、throughput 与 P95/P99。
- 容量估算从权重、KV、workspace、allocator overhead 四部分扣显存。
- full-forward 与 incremental/cache 路径必须做 logits parity。

## 15. 推荐验证协议

1. **Full-vs-cache parity**：逐 token logits 与 full forward 相同。
2. **Length sweep**：从短到长记录 TTFT/TPOT/KV bytes，验证复杂度趋势。
3. **Concurrency sweep**：增加并发直到 P99/SLA 或显存首先触顶，得到真实容量曲线。

验证结果建议至少记录：

| 类型 | 最低要求 |
|---|---|
| Correctness | reference parity / invariant / leakage test 至少一项 |
| Numerical | NaN/Inf、max/RMS、dtype 差异 |
| Performance | wall-clock、显存/bytes、P95/P99（系统题） |
| Quality | loss/perplexity/任务指标；结构改动必须做回归 |

## 16. 面试官真正想听到的“边界条件”

- **不要把渐进复杂度等同于实际速度**：kernel、batch、长度、硬件与 IO 会改变主导项。
- **不要把一种实现当成数学定义**：mask 语义、layout、fused kernel、cache allocator 都可能变化。
- **不要把单点 benchmark 当成普遍规律**：至少说明 $B,T,d,H$、dtype、device 和任务分布。
- **不要把“能运行”当成“正确”**：Transformer 很多 bug 会正常降 loss，但存在未来泄漏、错误广播或 cache position 错位。

## 17. Whiteboard / Coding 表达模板

当面试官要求现场推导或编码，可按以下顺序：

1. 写输入输出 shape 与符号定义；
2. 写最小核心公式，不先写优化；
3. 写 2–3 个 invariants/assertions；
4. 给 reference 实现或手算小例子；
5. 再讨论 fused kernel、cache、并行或量化；
6. 最后给测试与 benchmark 设计。

> **Strong Hire 信号**：不仅能解释“为什么”，还能说出“什么情况下不成立，以及我会怎么证明”。

<!-- V2-EXPANSION-END -->

---

← [Q066](../chapter-06/Q066.md) · [返回 100 题索引](../../guide/question-index.md) · [Q068](../chapter-07/Q068.md) →
