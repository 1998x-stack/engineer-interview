---
id: Q082
title: "Q082 Online Softmax 如何分块仍保持精确？"
chapter: 8
difficulty: "5/5"
tags: [attention]
---

# Q082 · Online Softmax 如何分块仍保持精确？

> **章节**：8. 长上下文、高性能 Attention 与 MoE  
> **难度**：5/5 · **频率**：高频 · **形式**：概念 / 推导 / 追问  
> **内容分层**：`PDF Core` 来自原 PDF 的观点重排；`Professional Expansion` 为在不改变 PDF 主结论基础上的原创扩展。

## 1. 面试官在考什么 — PDF Core

考察数值算法能力。

## 2. 30 秒回答 — PDF Core

> 每处理一块 logits，更新全局 max m=max(m_old,m_new)，并把旧的 exp-sum 与 output accumulator 乘 exp(m_old-m) 重新缩放，再加入新块贡献。这样最终结果与一次性 Softmax 数学等价。 l ← e^(m_old-m)l_old + e^(m_new-m)l_new

## 3. 核心公式 / Shape

$m'=\max(m,m_b),\;l'=e^{m-m'}l+\sum e^{x_b-m'}$。

## 4. 深度拆解 — PDF Core

- 最大值变化时必须重缩放旧贡献。
- 若只把各块独立 softmax 再拼接，会得到错误结果。
- 需要同时维护 denominator 和加权 V accumulator。

## 5. Professional Expansion：为什么这题值得深挖

Online Softmax 用新的全局 max 重缩放旧 partial sum：m=max(m_old,m_blk)，l=e^(m_old-m)l_old+Σe^(x-m)。

### 5.1 工程实现 / 系统视角

这是 FlashAttention exactness 的数值基础之一。

### 5.2 边界条件与反例

更新 accumulator 时也必须按相同缩放重标。

### 5.3 建议亲手做的验证实验

- 手算两块 logits 与一次性 softmax 对照。
- 记录至少一种 **correctness metric** 与一种 **performance metric**；不要只凭“代码能运行”判定实现正确。

### 5.4 3 分钟专业版答法

面试时可以按下面节奏展开：

> **第一句给结论**：每处理一块 logits，更新全局 max m=max(m_old,m_new)，并把旧的 exp-sum 与 output accumulator 乘 exp(m_old-m) 重新缩放，再加入新块贡献。这样最终结果与一次性 Softmax 数学等价。 l ← e^(m_old-m)l_old + e^(m_new-m)l_new  
> **第二层讲机制**：最大值变化时必须重缩放旧贡献。；若只把各块独立 softmax 再拼接，会得到错误结果。；需要同时维护 denominator 和加权 V accumulator。  
> **第三层补工程条件**：把本题放到真实 Tensor Shape、训练/推理阶段与硬件约束下重新检查；如果结论涉及“更快/更省/更稳定”，必须说明比较基线和适用条件。  
> **最后主动给验证**：用最小行为测试、数值 parity、profiler 或 ablation 证明，而不是停留在直觉。

### 5.5 复杂度与资源矩阵

| 维度 | 本题应如何分析 |
|---|---|
| 算法复杂度 | 先写理论 FLOPs / memory，再判断实现是否改变函数本身。 |
| 硬件复杂度 | HBM IO、SRAM tile、kernel launch、通信可能比 FLOPs 更决定 wall-clock。 |
| 质量风险 | 近似 attention / quantization / sparsity 需要单独做 quality regression。 |
| 长上下文 | T 增长同时放大 compute、KV、position extrapolation 与 TTFT。 |
| 主要验证 | profiler + memory trace + accuracy/perplexity + length sweep。 |

### 5.6 本题关键不变量

- 理论复杂度与实际 wall-clock 分开报告。
- 任何近似/量化/稀疏优化都必须做 quality regression。
- benchmark 必须给出硬件、dtype、batch、context/output length。

## 6. 面试中的推导顺序

1. **先定义**：一句话说明对象是什么、输入输出是什么。
2. **再公式**：写最小核心公式，并标出关键维度。
3. **解释 Why**：它解决前一代方案的什么瓶颈。
4. **算成本**：参数、FLOPs、activation/KV memory、IO/通信分别看。
5. **讲 Gotcha**：至少指出一个数值或实现陷阱。
6. **收束 Trade-off**：明确什么条件下它不一定更优。

## 7. 常见失分点 — PDF Core

- 对每个 block 独立 softmax 后直接 concat。

## 8. 面试官继续追问 — PDF Core

> 请推导两个 block 合并时 denominator 的更新公式。

### 扩展追问树

- 如果把关键超参数扩大 4 倍，**参数量 / FLOPs / 显存 / latency** 哪一项最先成为瓶颈？
- 如果在 **训练** 与 **增量推理** 两个阶段分别使用，本题结论有哪些不同？
- 如何写一个最小单元测试或 ablation，证明你的解释而不是只靠直觉？

## 9. 一语中的 — PDF Core

**每处理一块 logits，更新全局 max m=max(m_old,m_new)，并把旧的 exp-sum 与 output accumulator 乘 exp(m_old- m) 重新缩放，再加入新块贡献。**

## 10. 面试评分 Rubric

| 档位 | 面试表现 |
|---|---|
| 及格 | 能给出 PDF 的 30 秒主干，不出现概念性错误。 |
| 良好 | 能写核心公式/shape，并解释为什么这样设计。 |
| 优秀 | 能讨论复杂度、数值/工程 gotcha，并回答至少 2 层追问。 |
| 强 Hire 信号 | 能把问题连接到训练/推理/硬件约束，主动提出验证实验或 ablation。 |

> 本题标注难度：**5/5**。难度越高，越看重 trade-off 和验证方法，而不只是定义。

## 11. 相关题目

- [Q081 FlashAttention 为什么不需要存完整 Attention Matrix？](../chapter-08/Q081.md)
- [Q083 Sparse Attention 如何降低复杂度？](../chapter-08/Q083.md)

## 12. 延伸阅读

- [Attention Is All You Need](https://arxiv.org/abs/1706.03762)
- [FlashAttention](https://arxiv.org/abs/2205.14135)
- [FlashAttention-2](https://arxiv.org/abs/2307.08691)

<!-- V2-EXPANSION-START -->

## 13. 深入推导：把结论变成可验证的模型

Online softmax 的核心是可合并统计量。若旧块 max 为 $m_a$、归一和为 $l_a$，新块为 $m_b,l_b$，合并 max $m=\max(m_a,m_b)$，则 $l=e^{m_a-m}l_a+e^{m_b-m}l_b$。输出 accumulator 也按同样系数重标度。

这保证分块顺序下与整行稳定 softmax 数学等价，只存在浮点舍入差异。面试能写出这个 rescaling 就很加分。

### 13.1 本章心智模型

> 把高效 Transformer 放到 GPU/通信/稀疏计算上讨论，区分算法复杂度和系统效率。

这意味着面试时不要停在术语层。每个结论至少回答四个问题：**成立所需假设是什么？张量/概率对象是什么？成本落在哪里？怎样设计反例或单测证伪？**

## 14. 题目特异的工程 Checklist

**attention 检查表**

- 写出 Q/K/V 与 score 的**完整 shape**，尤其区分 $T_q$、$T_k$、$H_q$、$H_{kv}$。
- 明确 softmax 的归一化轴、mask 语义与 fully-masked row 行为。
- 用一个朴素 reference implementation 对 fused/optimized kernel 做数值 parity。
- 性能分析同时报告 FLOPs、HBM bytes、kernel launch 与是否 materialize $T^2$ 中间量。

## 15. 推荐验证协议

1. **Reference parity**：用小尺寸 FP32 手写 attention 与 `scaled_dot_product_attention` 对比。
2. **Mask invariance**：改变被 mask token 的内容，合法 query 输出不应变化。
3. **Probability invariant**：非 fully-masked 行的 attention probability sum 应约为 1。

验证结果建议至少记录：

| 类型 | 最低要求 |
|---|---|
| Correctness | reference parity / invariant / leakage test 至少一项 |
| Numerical | NaN/Inf、max/RMS、dtype 差异 |
| Performance | wall-clock、显存/bytes、P95/P99（系统题） |
| Quality | loss/perplexity/任务指标；结构改动必须做回归 |

## 16. 面试官真正想听到的“边界条件”

- **不要把渐进复杂度等同于实际速度**：kernel、batch、长度、硬件与 IO 会改变主导项。
- **不要把一种实现当成数学定义**：mask 语义、layout、fused kernel、cache allocator 都可能变化。
- **不要把单点 benchmark 当成普遍规律**：至少说明 $B,T,d,H$、dtype、device 和任务分布。
- **不要把“能运行”当成“正确”**：Transformer 很多 bug 会正常降 loss，但存在未来泄漏、错误广播或 cache position 错位。

## 17. Whiteboard / Coding 表达模板

当面试官要求现场推导或编码，可按以下顺序：

1. 写输入输出 shape 与符号定义；
2. 写最小核心公式，不先写优化；
3. 写 2–3 个 invariants/assertions；
4. 给 reference 实现或手算小例子；
5. 再讨论 fused kernel、cache、并行或量化；
6. 最后给测试与 benchmark 设计。

> **Strong Hire 信号**：不仅能解释“为什么”，还能说出“什么情况下不成立，以及我会怎么证明”。

<!-- V2-EXPANSION-END -->

---

← [Q081](../chapter-08/Q081.md) · [返回 100 题索引](../../guide/question-index.md) · [Q083](../chapter-08/Q083.md) →
