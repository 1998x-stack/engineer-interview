---
id: Q100
title: "Q100 System Design：用 Transformer 设计文本分类系统"
chapter: 9
difficulty: "3/5"
tags: [system-design]
---

# Q100 · System Design：用 Transformer 设计文本分类系统

> **章节**：9. Coding / Debug / System Design  
> **难度**：3/5 · **频率**：高频 · **形式**：概念 / 推导 / 追问  
> **内容分层**：`PDF Core` 来自原 PDF 的观点重排；`Professional Expansion` 为在不改变 PDF 主结论基础上的原创扩展。

## 1. 面试官在考什么 — PDF Core

考察从模型到数据、指标、部署完整链路。

## 2. 30 秒回答 — PDF Core

> 先定义标签与业务代价，再设计 tokenizer+encoder+pooling+classifier；训练处理类别不平衡与校准，评估同时看 precision/recall/F1、calibration、latency 与 drift。

## 3. 核心公式 / Shape

分类系统：$text\to tokenizer\to encoder\to pooling\to classifier\to calibrated\;decision$。

## 4. 深度拆解 — PDF Core

- 长文本可能需要 chunking/hierarchical pooling。
- CLS/mean pooling/attention pooling 要用验证集选择。
- 类别阈值应按业务 cost 调整，不一定固定 0.5。
- 线上监控 input drift 与 confidence drift。

## 5. Professional Expansion：为什么这题值得深挖

系统设计应从业务目标/标签代价开始，再到数据、tokenizer、encoder/pooling/head、训练、阈值校准、线上 SLA 和 drift 监控。

### 5.1 工程实现 / 系统视角

长文、极度不平衡、OOD 和解释需求会改变方案。

### 5.2 边界条件与反例

‘BERT+Linear’只覆盖模型，不覆盖系统。

### 5.3 建议亲手做的验证实验

- 为 0.1% 正例设计 PR-AUC/Recall@precision 阈值策略。
- 记录至少一种 **correctness metric** 与一种 **performance metric**；不要只凭“代码能运行”判定实现正确。

### 5.4 3 分钟专业版答法

面试时可以按下面节奏展开：

> **第一句给结论**：先定义标签与业务代价，再设计 tokenizer+encoder+pooling+classifier；训练处理类别不平衡与校准，评估同时看 precision/recall/F1、calibration、latency 与 drift。  
> **第二层讲机制**：长文本可能需要 chunking/hierarchical pooling。；CLS/mean pooling/attention pooling 要用验证集选择。；类别阈值应按业务 cost 调整，不一定固定 0.5。  
> **第三层补工程条件**：把本题放到真实 Tensor Shape、训练/推理阶段与硬件约束下重新检查；如果结论涉及“更快/更省/更稳定”，必须说明比较基线和适用条件。  
> **最后主动给验证**：用最小行为测试、数值 parity、profiler 或 ablation 证明，而不是停留在直觉。

### 5.5 复杂度与资源矩阵

| 维度 | 本题应如何分析 |
|---|---|
| Correctness | 先定义 shape/mask/finite/parity 等不变量，再写实现。 |
| 复杂度 | 避免隐藏的 `torch.cat`、contiguous copy、重复 materialization。 |
| 测试 | 优先行为级测试：future leakage、full-vs-cache、tiny overfit。 |
| 可维护性 | 开发态 fail-fast assertions；生产态转化为静态约束或 kernel contract。 |
| 主要验证 | pytest 参数化 B/T/H/GQA/padding/dtype/device 边界。 |

### 5.6 本题关键不变量

- 先失败于错误不变量，而不是让错误广播到后续层。
- 单元测试覆盖 B=1、T=1、H=1、padding/GQA 等边界。
- 优化版本必须与 reference/oracle 做 parity。

## 6. 面试中的推导顺序

1. **先定义**：一句话说明对象是什么、输入输出是什么。
2. **再公式**：写最小核心公式，并标出关键维度。
3. **解释 Why**：它解决前一代方案的什么瓶颈。
4. **算成本**：参数、FLOPs、activation/KV memory、IO/通信分别看。
5. **讲 Gotcha**：至少指出一个数值或实现陷阱。
6. **收束 Trade-off**：明确什么条件下它不一定更优。

## 7. 常见失分点 — PDF Core

- 只说“BERT+Linear+CrossEntropy”。

## 8. 面试官继续追问 — PDF Core

> 如果正样本只有 0.1%，你会优先优化哪些指标与采样策略？

### 扩展追问树

- 如果把关键超参数扩大 4 倍，**参数量 / FLOPs / 显存 / latency** 哪一项最先成为瓶颈？
- 如果在 **训练** 与 **增量推理** 两个阶段分别使用，本题结论有哪些不同？
- 如何写一个最小单元测试或 ablation，证明你的解释而不是只靠直觉？

## 9. 一语中的 — PDF Core

**先定义标签与业务代价，再设计 tokenizer + encoder + pooling + classifier；训练处理类别不平衡与校准，评估同时看 precision/recall/F1、calibration、latency 与 drift。**

## 10. 面试评分 Rubric

| 档位 | 面试表现 |
|---|---|
| 及格 | 能给出 PDF 的 30 秒主干，不出现概念性错误。 |
| 良好 | 能写核心公式/shape，并解释为什么这样设计。 |
| 优秀 | 能讨论复杂度、数值/工程 gotcha，并回答至少 2 层追问。 |
| 强 Hire 信号 | 能把问题连接到训练/推理/硬件约束，主动提出验证实验或 ablation。 |

> 本题标注难度：**3/5**。难度越高，越看重 trade-off 和验证方法，而不只是定义。

## 11. 相关题目

- [Q045 Encoder-only、Decoder-only、Encoder-Decoder 的本质区别？](../chapter-05/Q045.md)
- [Q056 Padding token 为什么不能参与 loss？](../chapter-06/Q056.md)
- [Q076 LLM Serving 的关键 latency 指标有哪些？](../chapter-07/Q076.md)

## 12. 延伸阅读

- [Attention Is All You Need](https://arxiv.org/abs/1706.03762)

<!-- V2-EXPANSION-START -->

## 13. 深入推导：把结论变成可验证的模型

文本分类系统不能只回答“BERT + Linear”。应先定义 label ontology、长文本策略、类别不平衡、延迟/SLA，再选 encoder、pooling、loss、threshold 和 calibration。离线指标至少包括 macro/micro F1、PR-AUC/ROC-AUC（视任务）和 calibration。

生产闭环还要有 drift、hard negative、阈值按业务成本调整、版本回滚与错误分析。长文本若截断会系统性漏信息，应比较 hierarchical/chunk pooling 或长上下文 encoder。

### 13.1 本章心智模型

> 把知识转成可验证代码：先不变量与 reference，再优化；System Design 必须有容量和回归闭环。

这意味着面试时不要停在术语层。每个结论至少回答四个问题：**成立所需假设是什么？张量/概率对象是什么？成本落在哪里？怎样设计反例或单测证伪？**

## 14. 题目特异的工程 Checklist

**system-design 检查表**

- 从需求/SLA 与数据分布出发，不从模型名字出发。
- 给出数据流、模型、容量、瓶颈、监控、回滚的完整生命周期。
- 至少估算一项显存/带宽和一项 P95 latency/throughput。
- 离线 quality 指标与线上业务指标必须建立映射。

## 15. 推荐验证协议

1. **Parameterized unit tests**：覆盖不同 B/T/H/dtype/mask。
2. **Behavioural test**：测试未来泄漏、padding invariance、cache reset 等语义。
3. **Oracle comparison**：优化代码与朴素 reference 比较输出和梯度。

验证结果建议至少记录：

| 类型 | 最低要求 |
|---|---|
| Correctness | reference parity / invariant / leakage test 至少一项 |
| Numerical | NaN/Inf、max/RMS、dtype 差异 |
| Performance | wall-clock、显存/bytes、P95/P99（系统题） |
| Quality | loss/perplexity/任务指标；结构改动必须做回归 |

## 16. 面试官真正想听到的“边界条件”

- **不要把渐进复杂度等同于实际速度**：kernel、batch、长度、硬件与 IO 会改变主导项。
- **不要把一种实现当成数学定义**：mask 语义、layout、fused kernel、cache allocator 都可能变化。
- **不要把单点 benchmark 当成普遍规律**：至少说明 $B,T,d,H$、dtype、device 和任务分布。
- **不要把“能运行”当成“正确”**：Transformer 很多 bug 会正常降 loss，但存在未来泄漏、错误广播或 cache position 错位。

## 17. Whiteboard / Coding 表达模板

当面试官要求现场推导或编码，可按以下顺序：

1. 写输入输出 shape 与符号定义；
2. 写最小核心公式，不先写优化；
3. 写 2–3 个 invariants/assertions；
4. 给 reference 实现或手算小例子；
5. 再讨论 fused kernel、cache、并行或量化；
6. 最后给测试与 benchmark 设计。

> **Strong Hire 信号**：不仅能解释“为什么”，还能说出“什么情况下不成立，以及我会怎么证明”。

<!-- V2-EXPANSION-END -->

---

← [Q099](../chapter-09/Q099.md) · [返回 100 题索引](../../guide/question-index.md)
